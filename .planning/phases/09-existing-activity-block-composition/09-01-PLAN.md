---
phase: 09-existing-activity-block-composition
plan: "01"
type: execute
wave: 1
depends_on:
  - "08-02"
files_modified:
  - App_Code/Common/AIActivityPlanDraftHelper.cs
  - App_Code/Common/AIActivityPlanSavedDraftHelper.cs
  - teacher/aiprovider_api.ashx
  - Tests/CommonLogicTests/CommonLogicTests.cs
  - Tests/TeacherRegressionTests/TeacherRegressionTests.cs
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
must_haves:
  truths:
    - Full-lesson blocks keep the Phase 8 stable envelope and add minimal typed payloads for `quiz`, `resource-study`, and `webCourseware`.
    - Helper validation fails closed when required legacy fields for any supported existing activity type are missing or malformed.
    - The phase stays preview-only and does not create `Mission`, `Exam`, or `ListMenu` rows.
  artifacts:
    - path: App_Code/Common/AIActivityPlanDraftHelper.cs
      provides: Full-lesson block parser and validation entry point to extend with typed payload schemas.
    - path: App_Code/Common/AIActivityPlanSavedDraftHelper.cs
      provides: Saved-draft envelope logic that must preserve typed block payloads.
    - path: teacher/aiprovider_api.ashx
      provides: Authenticated full-lesson generate/regenerate/save/load route boundary.
    - path: Tests/CommonLogicTests/CommonLogicTests.cs
      provides: Contract and validation coverage for typed block payloads.
    - path: Tests/TeacherRegressionTests/TeacherRegressionTests.cs
      provides: Source-lock coverage for preview-only handler behavior and supported activity types.
  key_links:
    - from: teacher/aiprovider_api.ashx
      to: App_Code/Common/AIActivityPlanDraftHelper.cs
      via: full-lesson block parsing, validation, and regenerate flow.
      pattern: fullLessonGenerate|fullLessonRegenerateBlock|ParseDraft
    - from: App_Code/Common/AIActivityPlanSavedDraftHelper.cs
      to: App_Code/Common/AIActivityPlanDraftHelper.cs
      via: saved-draft round-trip of the server-owned full-lesson DTO.
      pattern: SaveDraft|LoadDraft|Serialize
---

<objective>
Codify the minimal brownfield payload contracts for Phase 9 so full-lesson draft
blocks can safely represent existing quiz, resource-study, and web courseware
activities without publishing them yet.

Purpose: COMP-01, COMP-02, and COMP-03 all depend on a stable server-owned
contract first. This plan defines the typed payload shapes and validation rules
that Phase 9 preview and later publish work will rely on.
Output: typed block payload schemas, fail-closed validation, handler wiring for
the supported types, saved-draft compatibility, and focused tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-existing-activity-block-composition/09-CONTEXT.md
@.planning/phases/09-existing-activity-block-composition/09-DISCUSSION-LOG.md
@.planning/phases/08-full-lesson-draft-orchestration/08-CONTEXT.md
@App_Code/Common/AIActivityPlanDraftHelper.cs
@App_Code/Common/AIActivityPlanSavedDraftHelper.cs
@teacher/aiprovider_api.ashx
@Tests/CommonLogicTests/CommonLogicTests.cs
@Tests/TeacherRegressionTests/TeacherRegressionTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed payload schemas and validation for supported existing activity blocks</name>
  <files>App_Code/Common/AIActivityPlanDraftHelper.cs, App_Code/Common/AIActivityPlanSavedDraftHelper.cs, Tests/CommonLogicTests/CommonLogicTests.cs</files>
  <read_first>
    App_Code/Common/AIActivityPlanDraftHelper.cs
    App_Code/Common/AIActivityPlanSavedDraftHelper.cs
    Tests/CommonLogicTests/CommonLogicTests.cs
  </read_first>
  <behavior>
    - Test 1: `quiz` blocks require a minimal assessment payload and are rejected when key legacy fields are missing
    - Test 2: `resource-study` blocks require mission-reading content fields and preserve the stable full-lesson block envelope
    - Test 3: `webCourseware` blocks require ware-style homepage fields and survive saved-draft serialization round-trips
  </behavior>
  <action>Per D-04, D-05, D-06, and T-9-01, extend the Phase 8 block contract with the smallest safe type-specific payloads for `quiz`, `resource-study`, and `webCourseware`. Reuse brownfield field names where practical so later publish fan-out can stay shallow. Add helper validation that rejects malformed or partial typed payloads instead of accepting half-valid blocks. Keep the old Phase 8 stable block metadata intact and avoid introducing client-only alternate shapes.</action>
  <acceptance_criteria>
    - common helper code contains distinct validation or parsing branches for `quiz`, `resource-study`, and `webCourseware`
    - required typed payload fields are pinned in tests and cannot be silently omitted
    - saved-draft serialization preserves typed payload content under the existing block envelope
    - `Tests/CommonLogicTests/CommonLogicTests.cs` contains test names including `FullLesson` or `Composed`
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter FullLesson</automated>
  </verify>
  <done>The codebase has a test-protected typed block contract for the three supported existing activity types.</done>
</task>

<task type="auto">
  <name>Task 2: Extend authenticated full-lesson generation and regeneration to use the typed block payloads</name>
  <files>teacher/aiprovider_api.ashx, App_Code/Common/AIActivityPlanDraftHelper.cs, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    teacher/aiprovider_api.ashx
    App_Code/Common/AIActivityPlanDraftHelper.cs
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
  </read_first>
  <action>Per D-01, D-02, D-03, D-09, and T-9-02, update the authenticated full-lesson generate and block-regenerate flow so the server can return typed `quiz`, `resource-study`, and `webCourseware` payloads inside the existing full-lesson DTO. Keep authorization and preview-only behavior parallel to the current Phase 8 full-lesson routes. Add regression tests that pin support for the three activity types, preserve `fullLesson*` action names, and confirm Phase 9 still does not create real brownfield rows or invoke publish fan-out.</action>
  <acceptance_criteria>
    - `teacher/aiprovider_api.ashx` continues to use authenticated `fullLesson*` actions for generate and regenerate
    - the same file contains evidence of the three supported Phase 9 activity types
    - no Phase 9 handler path creates `Mission`, `Exam`, or `ListMenu` rows
    - `Tests/TeacherRegressionTests/TeacherRegressionTests.cs` includes assertions for Phase 9 type support or preview-only behavior
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter CourseEdit</automated>
  </verify>
  <done>The server can safely return and preserve typed existing-activity blocks while staying inside the Phase 9 preview boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI/provider response -> full-lesson draft parser | model-produced JSON crosses into privileged server validation and must not produce malformed typed blocks |
| saved full-lesson draft -> later preview/regenerate flow | persisted typed payloads could drift or lose required legacy fields between requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | T | typed block parser | mitigate | Reject malformed or partial `quiz`, `resource-study`, and `webCourseware` payloads through dedicated helper validation and unit tests |
| T-9-02 | S | `fullLesson*` typed generation flow | mitigate | Reuse existing teacher/course authorization while adding support for the three Phase 9 activity types |
| T-9-03 | T | preview-only boundary | mitigate | Keep Phase 9 limited to DTO generation and saved-draft persistence, with source-lock tests against publish or row-creation behavior |
</threat_model>

<verification>
- Run `dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter FullLesson`.
- Run `dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter CourseEdit`.
- Read `teacher/aiprovider_api.ashx` and confirm the supported type additions remain preview-only and authorization-safe.
</verification>

<success_criteria>
- The server owns minimal typed payload contracts for `quiz`, `resource-study`, and `webCourseware`.
- Malformed Phase 9 block payloads fail closed.
- Phase 9 generation and regenerate flows remain preview-only and teacher-authorized.
</success_criteria>

<output>
After completion, create `.planning/phases/09-existing-activity-block-composition/09-existing-activity-block-composition-01-SUMMARY.md`
</output>
