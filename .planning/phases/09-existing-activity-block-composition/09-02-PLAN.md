---
phase: 09-existing-activity-block-composition
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - teacher/courseedit.aspx
  - js/courseedit.js
  - Tests/TeacherRegressionTests/TeacherRegressionTests.cs
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
must_haves:
  truths:
    - The course editor renders type-aware preview cards for `quiz`, `resource-study`, and `webCourseware` inside the existing full-lesson draft experience.
    - Each supported block preview surfaces enough brownfield summary data for a teacher to recognize what the AI selected without opening a separate page.
    - Remove, regenerate, save, and resume continue to operate on the server-owned full-lesson DTO and do not mutate `mcontent` or brownfield tables.
  artifacts:
    - path: teacher/courseedit.aspx
      provides: Existing assistant shell and preview container for the composed block cards.
    - path: js/courseedit.js
      provides: Browser rendering, saved-draft resume, and block-level remove/regenerate interactions.
    - path: Tests/TeacherRegressionTests/TeacherRegressionTests.cs
      provides: Source-lock coverage for type-aware preview summaries and no-auto-publish behavior.
  key_links:
    - from: teacher/courseedit.aspx
      to: js/courseedit.js
      via: full-lesson preview shell and block-card action controls.
      pattern: activity-plan|block|preview
    - from: js/courseedit.js
      to: teacher/aiprovider_api.ashx
      via: block-level regenerate, save, and resume against the typed full-lesson DTO.
      pattern: fullLessonRegenerateBlock|fullLessonSaveDraft|fullLessonLoadDraft
---

<objective>
Upgrade the teacher course editor so Phase 9 typed existing-activity blocks are
recognizable and reviewable inside the existing full-lesson preview workflow.

Purpose: the Phase 9 value is not just that the server knows the type, but that
the teacher can see AI choose a quiz, resource-study, or web courseware block
at the right lesson moment. This plan lands the teacher-visible preview layer
on top of the typed Phase 9 contract.
Output: type-aware preview cards, supported summary rendering, preserved block-
level controls, and regression coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-existing-activity-block-composition/09-CONTEXT.md
@.planning/phases/09-existing-activity-block-composition/09-DISCUSSION-LOG.md
@teacher/courseedit.aspx
@js/courseedit.js
@teacher/aiprovider_api.ashx
@Tests/TeacherRegressionTests/TeacherRegressionTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render supported existing activity types with type-aware preview summaries</name>
  <files>teacher/courseedit.aspx, js/courseedit.js, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    teacher/courseedit.aspx
    js/courseedit.js
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
  </read_first>
  <action>Per D-07 and T-9-04, keep the current course editor assistant shell but evolve the block-card preview so supported existing activity types render richer summaries. In `js/courseedit.js`, render server-owned summaries for `quiz`, `resource-study`, and `webCourseware` using the typed payloads from Plan 01, and label them clearly alongside the stable block metadata from Phase 8. In `teacher/courseedit.aspx`, add only the smallest markup or style adjustments needed to distinguish these cards without introducing a new page or preview route.</action>
  <acceptance_criteria>
    - `js/courseedit.js` contains preview rendering branches or helpers for `quiz`, `resource-study`, and `webCourseware`
    - each supported card surfaces type-specific summary data beyond the generic block metadata
    - `teacher/courseedit.aspx` contains the UI hooks or styles needed for the richer block-card presentation
    - `Tests/TeacherRegressionTests/TeacherRegressionTests.cs` includes assertions for supported preview strings or wiring
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter CourseEdit</automated>
  </verify>
  <done>Teachers can tell which supported brownfield activity type AI selected and what it contains before any publish step exists.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve block-level refinement and saved-draft behavior with typed previews</name>
  <files>js/courseedit.js, teacher/courseedit.aspx, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    js/courseedit.js
    teacher/courseedit.aspx
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
  </read_first>
  <action>Per D-08, D-09, and T-9-05, keep remove, regenerate, save, and resume scoped to the server-owned full-lesson DTO even after the richer type-aware previews land. Ensure resumed drafts re-render the same type-aware cards, and keep Phase 9 firmly preview-first by avoiding writes into `mcontent` or any brownfield publish tables. Reuse the current Phase 8 block-keyed interaction model instead of inventing a second client-side state shape for typed previews.</action>
  <acceptance_criteria>
    - block-level remove and regenerate still operate by `blockKey` after type-aware preview rendering is added
    - saved and resumed drafts preserve the same typed preview summaries
    - no Phase 9 preview interaction path writes directly into `mcontent` or publish-oriented tables
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter CourseEdit</automated>
    <manual>Authenticated browser UAT: generate a composed draft containing supported existing activity types, save it, reload it, remove one block, regenerate another, and confirm the remaining typed preview cards stay intact while the lesson editor body remains unchanged.</manual>
  </verify>
  <done>The teacher can refine typed existing-activity blocks one at a time without losing the rest of the draft or silently publishing anything.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-owned typed block DTO -> browser preview | teacher-visible summaries must faithfully represent the supported activity types and not drift into client-invented semantics |
| browser block actions -> resumed draft state | remove/regenerate/save/load operations must preserve typed block data for untouched blocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-04 | I | typed preview UI | mitigate | Render supported type summaries from the server DTO only and pin required preview strings in source tests |
| T-9-05 | T | typed block refinement flow | mitigate | Keep block-level actions keyed by `blockKey` and preserve untouched typed payloads through save/load/regenerate cycles |
| T-9-06 | T | preview-only course editor boundary | mitigate | Keep typed preview interactions separate from `mcontent` and brownfield publish tables, with regression checks for no-auto-publish behavior |
</threat_model>

<verification>
- Run `dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter CourseEdit`.
- Manually verify type-aware preview, save/resume, remove, and regenerate behavior in an authenticated teacher session.
</verification>

<success_criteria>
- The teacher sees recognizable quiz, resource-study, and web courseware preview cards in the existing course editor flow.
- Save/resume/remove/regenerate preserve the typed preview state for untouched blocks.
- Phase 9 stays preview-first and does not auto-write lesson content or publish data.
</success_criteria>

<output>
After completion, create `.planning/phases/09-existing-activity-block-composition/09-existing-activity-block-composition-02-SUMMARY.md`
</output>
