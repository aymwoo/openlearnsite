---
phase: 08-full-lesson-draft-orchestration
plan: "01"
type: execute
wave: 1
depends_on:
  - "07-02"
files_modified:
  - App_Code/Common/AIActivityPlanDraftHelper.cs
  - App_Code/Common/AIActivityPlanSavedDraftHelper.cs
  - App_Code/Model/CourseActivityPlanDraft.cs
  - App_Code/Dal/CourseActivityPlanDraft.cs
  - teacher/aiprovider_api.ashx
  - Tests/CommonLogicTests/CommonLogicTests.cs
  - Tests/TeacherRegressionTests/TeacherRegressionTests.cs
autonomous: true
requirements:
  - ORCH-01
  - ORCH-03
must_haves:
  truths:
    - Full-lesson draft parsing is block-based and rejects malformed or partial payloads.
    - Save/load/delete/regenerate full-lesson actions remain teacher-authorized and parallel to the shipped single-activity actions.
    - The shipped single-activity publish path stays untouched in this phase.
  artifacts:
    - path: App_Code/Common/AIActivityPlanDraftHelper.cs
      provides: Existing draft parser baseline to extend or parallelize safely.
    - path: App_Code/Common/AIActivityPlanSavedDraftHelper.cs
      provides: Saved-draft envelope logic for one current course draft.
    - path: teacher/aiprovider_api.ashx
      provides: Authenticated `fullLesson*` handler cases.
    - path: Tests/CommonLogicTests/CommonLogicTests.cs
      provides: Full-lesson helper and parser validation coverage.
    - path: Tests/TeacherRegressionTests/TeacherRegressionTests.cs
      provides: Source regressions for handler wiring and brownfield route safety.
  key_links:
    - from: teacher/aiprovider_api.ashx
      to: App_Code/Common/AIActivityPlanDraftHelper.cs
      via: full-lesson request parsing and validation.
      pattern: fullLesson|ParseDraft
    - from: teacher/aiprovider_api.ashx
      to: App_Code/Dal/CourseActivityPlanDraft.cs
      via: authenticated save/load/delete of one current draft.
      pattern: SaveDraft|LoadDraft|DeleteDraft
---

<objective>
Establish the Phase 8 full-lesson draft contract and authenticated handler
foundation without destabilizing the shipped single-activity planning flow.

Purpose: ORCH-01 and the persistence part of ORCH-03 depend on a stable typed
contract first. This plan creates the server-owned shape that later UI work can
render and manipulate safely.
Output: full-lesson helper/parser foundation, parallel `fullLesson*` handler
actions, draft persistence compatibility, and focused tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-full-lesson-draft-orchestration/08-CONTEXT.md
@.planning/phases/08-full-lesson-draft-orchestration/08-RESEARCH.md
@.planning/phases/08-full-lesson-draft-orchestration/08-VALIDATION.md
@App_Code/Common/AIActivityPlanDraftHelper.cs
@App_Code/Common/AIActivityPlanSavedDraftHelper.cs
@App_Code/Model/CourseActivityPlanDraft.cs
@App_Code/Dal/CourseActivityPlanDraft.cs
@teacher/aiprovider_api.ashx
@Tests/CommonLogicTests/CommonLogicTests.cs
@Tests/TeacherRegressionTests/TeacherRegressionTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a typed full-lesson draft parser and validator</name>
  <files>App_Code/Common/AIActivityPlanDraftHelper.cs, App_Code/Common/AIActivityPlanSavedDraftHelper.cs, Tests/CommonLogicTests/CommonLogicTests.cs</files>
  <read_first>
    App_Code/Common/AIActivityPlanDraftHelper.cs
    App_Code/Common/AIActivityPlanSavedDraftHelper.cs
    Tests/CommonLogicTests/CommonLogicTests.cs
  </read_first>
  <behavior>
    - Test 1: malformed full-lesson payloads are rejected instead of silently producing partial blocks
    - Test 2: valid payloads require stable `blockKey`, ordered `sort`, `blockType`, `teachingPurpose`, and `lessonPosition`
    - Test 3: saved-draft serialization round-trips the server-owned full-lesson shape without falling back to prose-only sections
  </behavior>
  <action>Per D-01, D-02, and T-8-01, introduce the smallest safe full-lesson helper surface parallel to the existing section-based draft contract. Add parsing, validation, and saved-envelope support for a block-ordered full-lesson payload while keeping the old `activityPlan` contract intact. Prefer dedicated full-lesson DTOs or helper methods rather than overloading section keys like `activitySteps`. Add focused common-logic tests that pin the full-lesson schema and reject partial block data.</action>
  <acceptance_criteria>
    - common helper code contains a distinct full-lesson parse or validation path
    - required block metadata includes `blockKey`, `blockType`, `teachingPurpose`, and `lessonPosition`
    - `Tests/CommonLogicTests/CommonLogicTests.cs` contains test names including `FullLesson`
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter FullLesson</automated>
  </verify>
  <done>The codebase has a stable, test-protected full-lesson contract that later UI work can trust.</done>
</task>

<task type="auto">
  <name>Task 2: Add authenticated `fullLesson*` draft actions parallel to the shipped activity-plan actions</name>
  <files>teacher/aiprovider_api.ashx, App_Code/Model/CourseActivityPlanDraft.cs, App_Code/Dal/CourseActivityPlanDraft.cs, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    teacher/aiprovider_api.ashx
    App_Code/Model/CourseActivityPlanDraft.cs
    App_Code/Dal/CourseActivityPlanDraft.cs
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
  </read_first>
  <action>Per D-03, D-04, and T-8-02, extend the authenticated handler with `fullLessonGenerate`, `fullLessonRegenerateBlock`, `fullLessonDraftStatus`, `fullLessonSaveDraft`, `fullLessonLoadDraft`, and `fullLessonDeleteDraft`. Reuse the existing authorized-course and one-current-draft patterns, but keep the new actions parallel to the existing `activityPlan*` actions instead of rewriting them. Update draft persistence only as much as needed to hold the new full-lesson JSON envelope. Add regression tests that pin the new action names, their continued use of course authorization, and the fact that `activityPlanPublish` remains present and unmutated.</action>
  <acceptance_criteria>
    - `teacher/aiprovider_api.ashx` contains the six `fullLesson*` action cases
    - the same file keeps `activityPlanPublish` and the existing `activityPlan*` flow intact
    - draft persistence continues to support one current draft per course and teacher
    - `Tests/TeacherRegressionTests/TeacherRegressionTests.cs` contains test names including `FullLesson` or `CourseEdit`
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter CourseEdit</automated>
  </verify>
  <done>The server exposes a safe full-lesson draft API surface that coexists with the shipped single-activity planning flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher browser -> `teacher/aiprovider_api.ashx` | user-controlled draft JSON crosses into privileged teacher-authorized server code |
| full-lesson draft persistence -> later preview actions | malformed or drifted stored JSON could corrupt block-level review behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | T | full-lesson draft parser | mitigate | Reject malformed and partial block payloads through dedicated helper validation and unit tests |
| T-8-02 | S | `fullLesson*` handler actions | mitigate | Reuse existing teacher/course authorization before save/load/delete/regenerate work |
| T-8-05 | R | shipped single-activity flow | mitigate | Keep `activityPlan*` and `activityPlanPublish` source locks present while adding the new parallel contract |
</threat_model>

<verification>
- Run `dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter FullLesson`.
- Run `dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter CourseEdit`.
- Read `teacher/aiprovider_api.ashx` and confirm the new `fullLesson*` actions
  are parallel additions, not rewrites of `activityPlan*` behavior.
</verification>

<success_criteria>
- The server owns a typed full-lesson draft contract.
- Full-lesson save/load/delete/regenerate routes are authenticated and draft-
  safe.
- Existing single-activity planning and publish contracts remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/08-full-lesson-draft-orchestration/08-full-lesson-draft-orchestration-01-SUMMARY.md`
</output>
