---
phase: 07-submission-and-completion-tracking
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - App_Code/Bll/MenuWorks.cs
  - App_Code/Dal/MenuWorks.cs
  - App_Code/Bll/Works.cs
  - student/uploadwork.aspx.cs
  - student/uploadworkm.aspx.cs
  - Tests/CommonLogicTests/CommonLogicTests.cs
  - Tests/TeacherRegressionTests/TeacherRegressionTests.cs
autonomous: true
requirements:
  - SCT-01
  - SCT-02
must_haves:
  truths:
    - Successful AI activity submission records completion through the existing `MenuWorks` model.
    - Completion writes do not create duplicate student-menu completion rows.
    - Existing student and teacher-facing completion surfaces can distinguish completed from untouched activity entries.
  artifacts:
    - path: App_Code/Bll/MenuWorks.cs
      provides: Completion-record access pattern for the AI mission flow.
    - path: App_Code/Dal/MenuWorks.cs
      provides: Persistence rules for one student/menu completion record.
    - path: App_Code/Bll/Works.cs
      provides: Shared brownfield helpers if completion writes are centralized.
    - path: Tests/CommonLogicTests/CommonLogicTests.cs
      provides: Logic coverage for completion-record behavior.
  key_links:
    - from: student/uploadwork.aspx.cs
      to: App_Code/Bll/MenuWorks.cs
      via: completion write after successful first submission.
      pattern: MenuWorks|kbll.Add
    - from: student/Scm.master.cs
      to: App_Code/Bll/MenuWorks.cs
      via: completion visibility in menu state.
      pattern: GetMyLidCount
---

<objective>
Close the completion-tracking gap by making `MenuWorks`-based completion for AI
activities explicit, reliable, and verifiable.

Purpose: The milestone is not complete unless successful submission updates the
same classroom completion surfaces used elsewhere in the product. This plan
finishes that end-to-end link.
Output: completion-write hardening or helper extraction, tests, and manual UAT
for upload plus completed-state visibility.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-v1.1-MILESTONE-AUDIT.md
@.planning/phases/07-submission-and-completion-tracking/07-CONTEXT.md
@.planning/phases/07-submission-and-completion-tracking/07-RESEARCH.md
@App_Code/Bll/MenuWorks.cs
@App_Code/Dal/MenuWorks.cs
@App_Code/Bll/Works.cs
@student/uploadwork.aspx.cs
@student/uploadworkm.aspx.cs
@student/Scm.master.cs
@Tests/CommonLogicTests/CommonLogicTests.cs
@Tests/TeacherRegressionTests/TeacherRegressionTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden completion writes to one `MenuWorks` record per student activity</name>
  <files>App_Code/Bll/MenuWorks.cs, App_Code/Dal/MenuWorks.cs, App_Code/Bll/Works.cs, student/uploadwork.aspx.cs, student/uploadworkm.aspx.cs, Tests/CommonLogicTests/CommonLogicTests.cs</files>
  <read_first>
    App_Code/Bll/MenuWorks.cs
    App_Code/Dal/MenuWorks.cs
    App_Code/Bll/Works.cs
    student/uploadwork.aspx.cs
    student/uploadworkm.aspx.cs
    Tests/CommonLogicTests/CommonLogicTests.cs
  </read_first>
  <behavior>
    - Test 1: successful first submission records completion through `MenuWorks`
    - Test 2: later resubmission does not create duplicate completion rows
    - Test 3: completion writes remain on the existing brownfield model instead of a new AI table
  </behavior>
  <action>Per D-03 and D-04, inspect the current upload handlers and shared `Works` logic to ensure successful AI mission submission records one completion row per `Klid` + `Ksid`. If the existing handlers already do this safely, add tests and minimal guards. If the behavior is duplicated or brittle, extract the smallest shared helper under existing BLL classes so both upload handlers reuse the same completion-write rule. Add CommonLogic coverage that pins `MenuWorks` usage and rejects any new AI-specific completion store.</action>
  <acceptance_criteria>
    - completion logic continues to use `MenuWorks` with `Klid` and `Ksid`
    - `Tests/CommonLogicTests/CommonLogicTests.cs` contains test names including `MenuWorks` or `ActivityPlanCompletion`
    - no new AI-specific completion table or route is introduced
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter "FullyQualifiedName~MenuWorks|FullyQualifiedName~ActivityPlanCompletion"</automated>
  </verify>
  <done>The AI mission completion path is explicit, shared where needed, and protected against duplicate completion records.</done>
</task>

<task type="auto">
  <name>Task 2: Verify completed-state visibility across student flow surfaces</name>
  <files>student/Scm.master.cs, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    student/Scm.master.cs
    student/myinfo.aspx.cs
    App_Code/Bll/MenuWorks.cs
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
  </read_first>
  <action>Per D-06 and D-08, confirm that the existing completion surfaces can distinguish completed AI activity submissions from untouched entries. Add regression tests that pin the student menu's finish-state dependency on the current completion/work model, and document manual UAT for verifying the icon or completion change after a successful AI activity upload. Only add teacher- or student-surface tweaks if current behavior is not legible enough to demonstrate the milestone requirement.</action>
  <acceptance_criteria>
    - completion visibility remains tied to existing menu or summary surfaces
    - `Tests/TeacherRegressionTests/TeacherRegressionTests.cs` contains source assertions touching completion visibility
    - phase summary can include manual UAT for upload -> completed-state visibility
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter "FullyQualifiedName~ActivityPlanCompletion|FullyQualifiedName~Student"</automated>
    <manual>Authenticated browser UAT: publish one AI activity, submit a valid student work file, refresh the student menu or summary surface, and confirm the activity now appears completed instead of untouched.</manual>
  </verify>
  <done>The milestone has an end-to-end completion story that is visible to real users, not only hidden in database side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| successful upload -> completion persistence | A submission event becomes durable classroom completion state |
| completion persistence -> student menu/status surfaces | Stored completion changes learner-visible and teacher-visible state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | T | `MenuWorks` completion writes | mitigate | Reuse one brownfield completion rule and add tests that guard against duplicate rows |
| T-07-05 | I | Completion visibility | mitigate | Keep the current menu and summary surfaces backed by the same `MenuWorks` model instead of introducing diverging AI-specific status state |
| T-07-06 | R | End-to-end validation | mitigate | Pair targeted source tests with manual upload-and-refresh UAT for final completion visibility |
</threat_model>

<verification>
- Run the targeted `CommonLogicTests` and `TeacherRegressionTests` filters for
  completion behavior.
- Manually verify upload success and completed-state visibility on the student
  menu or summary surface.
</verification>

<success_criteria>
- Successful AI activity submission records completion through the existing
  classroom tracking model.
- Completion state can be distinguished from untouched entries on current
  surfaces.
- Phase 7 ends with explicit manual and automated validation coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/07-submission-and-completion-tracking/07-submission-and-completion-tracking-02-SUMMARY.md`
</output>
