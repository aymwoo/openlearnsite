---
phase: 07-submission-and-completion-tracking
plan: "01"
type: execute
wave: 1
depends_on:
  - "06-02"
files_modified:
  - student/showmission.aspx.cs
  - student/uploadwork.aspx.cs
  - student/uploadworkm.aspx.cs
  - Tests/TeacherRegressionTests/TeacherRegressionTests.cs
autonomous: true
requirements:
  - SCT-01
must_haves:
  truths:
    - Published AI activities submit through the existing upload mission flow.
    - Submission resolves the correct mission identity from the published menu row.
    - The mission page clearly communicates current submission availability and resubmit state.
  artifacts:
    - path: student/showmission.aspx.cs
      provides: Student-facing submission state logic for AI mission activities.
    - path: student/uploadwork.aspx.cs
      provides: Primary upload submission contract for the AI mission path.
    - path: student/uploadworkm.aspx.cs
      provides: Alternate upload-mode parity for the same AI mission contract.
    - path: Tests/TeacherRegressionTests/TeacherRegressionTests.cs
      provides: Source regression coverage for submission routing and status messaging.
  key_links:
    - from: student/showmission.aspx.cs
      to: student/uploadwork.aspx.cs
      via: existing upload shell and `lid`-based submission flow.
      pattern: uploadwork.aspx|lid
    - from: student/uploadwork.aspx.cs
      to: ListMenu/Mission/Works
      via: `lid` -> `Lxid` -> `Mid` submission resolution.
      pattern: GetModel\(|Lxid|AddWorkUp
---

<objective>
Pin and, if needed, lightly refine the student submission path for published AI
activities.

Purpose: The milestone audit identifies a broken teacher publish -> student
submit path. Phase 7 starts by proving that the current upload flow works for
the AI mission contract and by making the mission-page submission state clear.
Output: source-tested submission wiring and student-facing submission state.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-v1.1-MILESTONE-AUDIT.md
@.planning/phases/07-submission-and-completion-tracking/07-CONTEXT.md
@.planning/phases/07-submission-and-completion-tracking/07-RESEARCH.md
@student/showmission.aspx.cs
@student/uploadwork.aspx.cs
@student/uploadworkm.aspx.cs
@Tests/TeacherRegressionTests/TeacherRegressionTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the AI mission upload route to the existing submission handlers</name>
  <files>student/uploadwork.aspx.cs, student/uploadworkm.aspx.cs, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    student/uploadwork.aspx.cs
    student/uploadworkm.aspx.cs
    App_Code/Dal/AIActivityPlanPublisher.cs
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
  </read_first>
  <action>Per D-01, D-02, and D-07, keep published AI activities on the existing upload handlers rather than adding a new AI submission endpoint. Add or update regression tests that assert `student/uploadwork.aspx.cs` and `student/uploadworkm.aspx.cs` resolve the mission from the posted `lid`, read the linked `ListMenu` row, and create or update the correct `Works` record. If either handler needs a small guard to better align with the AI publish contract, keep the change minimal and preserve existing file-type and authorization behavior.</action>
  <acceptance_criteria>
    - `student/uploadwork.aspx.cs` contains `Request.QueryString["lid"]` and resolves `lmodel.Lxid`
    - `student/uploadworkm.aspx.cs` preserves the same `lid`-to-mission submission contract
    - `Tests/TeacherRegressionTests/TeacherRegressionTests.cs` contains test names including `UploadWork` or `ActivityPlanSubmission`
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter "FullyQualifiedName~UploadWork|FullyQualifiedName~ActivityPlanSubmission"</automated>
  </verify>
  <done>The AI mission now has a regression-pinned path into the existing submission handlers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make submission state explicit on the mission page</name>
  <files>student/showmission.aspx.cs, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    student/showmission.aspx.cs
    student/showtask.aspx.cs
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
  </read_first>
  <behavior>
    - Test 1: the mission page distinguishes not-yet-submitted, resubmittable, and teacher-locked states
    - Test 2: submission messaging still respects existing IP and prior-work restrictions
    - Test 3: upload affordances remain visible only when submission is actually allowed
  </behavior>
  <action>Per D-05 and D-07, tighten `student/showmission.aspx.cs` so the AI mission path clearly communicates current submission state through existing labels and panels. Reuse the existing brownfield logic for prior work, IP limits, and post-evaluation lockout instead of inventing a new AI state model. Add regression tests that pin the presence of these submission state branches and the continued use of the existing upload shell.</action>
  <acceptance_criteria>
    - `student/showmission.aspx.cs` contains explicit message branches for submitted and locked states
    - the page still gates `Panelswfupload` from server logic
    - `Tests/TeacherRegressionTests/TeacherRegressionTests.cs` contains source assertions for mission-page submission state
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter "FullyQualifiedName~ShowMission|FullyQualifiedName~ActivityPlanSubmission"</automated>
  </verify>
  <done>Students can tell whether they can submit, revise, or are blocked on the published AI activity page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student upload request -> upload handler | User-controlled file submissions enter privileged server and filesystem code |
| mission page server state -> student UI | Server submission state becomes the student's decision surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | S | AI mission upload contract | mitigate | Keep the published AI activity on the existing `lid`-resolved upload flow instead of adding a new weaker endpoint |
| T-07-02 | T | Submission state messaging | mitigate | Derive upload availability from existing server checks for prior work, IP limits, and evaluation lockout |
| T-07-03 | R | Submission path validation | mitigate | Add source regressions for `uploadwork` and `showmission` state branches before manual UAT |
</threat_model>

<verification>
- Run the targeted `TeacherRegressionTests` filters for upload and mission-page
  submission state assertions.
- Read `student/uploadwork.aspx.cs`, `student/uploadworkm.aspx.cs`, and
  `student/showmission.aspx.cs` to confirm the AI mission remains on the normal
  upload path.
</verification>

<success_criteria>
- Students can submit a result from the published AI activity flow.
- Submission is linked to the correct published activity identity.
- The student page communicates current submission state clearly enough to use
  the upload flow correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/07-submission-and-completion-tracking/07-submission-and-completion-tracking-01-SUMMARY.md`
</output>
