---
phase: 10-guided-inquiry-fallback-and-combined-publish
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - App_Code/Common/AIActivityPlanPublishContentBuilder.cs
  - App_Code/Common/AIActivityPlanSavedDraftHelper.cs
  - App_Code/Dal/AIActivityPlanPublisher.cs
  - App_Code/Bll/AIActivityPlanPublisher.cs
  - App_Code/Model/AIActivityPlanPublishRequest.cs
  - App_Code/Model/AIActivityPlanPublishResult.cs
  - teacher/aiprovider_api.ashx
  - js/courseedit.js
  - Tests/CommonLogicTests/CommonLogicTests.cs
  - Tests/TeacherRegressionTests/TeacherRegressionTests.cs
autonomous: true
requirements:
  - COMP-04
  - INQ-02
must_haves:
  truths:
    - Teachers can confirm one mixed full-lesson draft and publish it through one explicit combined action in the existing course editor.
    - Combined publish creates or updates the matching set of brownfield activity entries for `guidedInquiry`, `quiz`, `resource-study`, and `webCourseware` in draft order.
    - Republish stays idempotent at the block level by tracking server-owned publish links keyed by `blockKey`.
  artifacts:
    - path: App_Code/Dal/AIActivityPlanPublisher.cs
      provides: Current single-activity publish transaction to extend or parallelize for mixed full-lesson publish.
    - path: App_Code/Common/AIActivityPlanSavedDraftHelper.cs
      provides: Existing saved-draft record boundary where block-level publish links can be preserved.
    - path: teacher/aiprovider_api.ashx
      provides: Authenticated handler boundary for the new `fullLessonPublish` action.
    - path: js/courseedit.js
      provides: Existing teacher publish button wiring and current draft payload source.
    - path: Tests/CommonLogicTests/CommonLogicTests.cs
      provides: Publish content and idempotency-oriented logic coverage.
    - path: Tests/TeacherRegressionTests/TeacherRegressionTests.cs
      provides: Source-lock coverage for the new handler route, mixed publish wiring, and brownfield target evidence.
  key_links:
    - from: teacher/aiprovider_api.ashx
      to: App_Code/Dal/AIActivityPlanPublisher.cs
      via: authenticated combined publish entry for mixed full-lesson drafts.
      pattern: fullLessonPublish|Publish
    - from: App_Code/Dal/AIActivityPlanPublisher.cs
      to: App_Code/Common/AIActivityPlanSavedDraftHelper.cs
      via: blockKey-based publish-link persistence for create-or-update behavior.
      pattern: blockKey|Linked|DraftJson
---

<objective>
Turn the reviewed mixed full-lesson draft into one explicit teacher publish
action that safely creates or updates the matching classroom activities through
existing brownfield routes.

Purpose: `COMP-04` and the publish part of `INQ-02` depend on a teacher-visible,
transaction-safe combined publish path. This plan lands the server publish fan-
out, block-level link persistence, and course-editor publish wiring needed to
make mixed drafts executable.
Output: combined publish handler, mixed brownfield transaction, idempotent
block-level publish links, teacher publish UI wiring, and focused tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-guided-inquiry-fallback-and-combined-publish/10-CONTEXT.md
@.planning/phases/10-guided-inquiry-fallback-and-combined-publish/10-DISCUSSION-LOG.md
@App_Code/Common/AIActivityPlanPublishContentBuilder.cs
@App_Code/Common/AIActivityPlanSavedDraftHelper.cs
@App_Code/Dal/AIActivityPlanPublisher.cs
@App_Code/Bll/AIActivityPlanPublisher.cs
@App_Code/Model/AIActivityPlanPublishRequest.cs
@App_Code/Model/AIActivityPlanPublishResult.cs
@teacher/aiprovider_api.ashx
@js/courseedit.js
@teacher/courseedit.aspx
@student/showmission.aspx.cs
@teacher/wareadd.aspx.cs
@exam/examadd.aspx.cs
@Tests/CommonLogicTests/CommonLogicTests.cs
@Tests/TeacherRegressionTests/TeacherRegressionTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a transaction-safe mixed full-lesson publish core with block-level link persistence</name>
  <files>App_Code/Dal/AIActivityPlanPublisher.cs, App_Code/Bll/AIActivityPlanPublisher.cs, App_Code/Common/AIActivityPlanPublishContentBuilder.cs, App_Code/Common/AIActivityPlanSavedDraftHelper.cs, App_Code/Model/AIActivityPlanPublishRequest.cs, App_Code/Model/AIActivityPlanPublishResult.cs, Tests/CommonLogicTests/CommonLogicTests.cs</files>
  <read_first>
    App_Code/Dal/AIActivityPlanPublisher.cs
    App_Code/Common/AIActivityPlanPublishContentBuilder.cs
    App_Code/Common/AIActivityPlanSavedDraftHelper.cs
    App_Code/Model/AIActivityPlanPublishRequest.cs
    App_Code/Model/AIActivityPlanPublishResult.cs
    Tests/CommonLogicTests/CommonLogicTests.cs
  </read_first>
  <behavior>
    - Test 1: a mixed draft containing `guidedInquiry`, `quiz`, `resource-study`, and `webCourseware` resolves into publishable per-block operations in draft order
    - Test 2: republishing the same `blockKey` set updates the matching publish links instead of blindly creating duplicates
    - Test 3: unsupported or malformed block payloads fail the combined publish request instead of partially publishing a lesson
  </behavior>
  <action>Per D-04, D-05, D-06, D-07, and T-10-04, extend or parallelize the current publish core so it can accept a full-lesson draft and fan it out into the matching brownfield entities through one transaction. Reuse existing activity-specific contracts where practical, but keep publish-link persistence server-owned and keyed by `blockKey` so create-or-update behavior does not depend on title matching. Add focused tests for ordering, fail-closed validation, and idempotent republish behavior.</action>
  <acceptance_criteria>
    - publish core contains an explicit mixed full-lesson path rather than overloading the old single-activity assumptions invisibly
    - the implementation shows how `guidedInquiry`, `quiz`, `resource-study`, and `webCourseware` each resolve to their brownfield target type
    - block-level publish links are preserved in a server-owned structure keyed by `blockKey`
    - `Tests/CommonLogicTests/CommonLogicTests.cs` includes test names including `FullLessonPublish`, `Composed`, or `GuidedInquiry`
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter FullLesson</automated>
  </verify>
  <done>The server has a safe combined publish core for reviewed mixed full-lesson drafts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire one explicit `fullLessonPublish` action into the existing course editor workflow</name>
  <files>teacher/aiprovider_api.ashx, js/courseedit.js, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    teacher/aiprovider_api.ashx
    js/courseedit.js
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
  </read_first>
  <action>Per D-08, D-10, D-11, and T-10-05, add `fullLessonPublish` to the authenticated handler and switch the course editor publish button to send the current reviewed full-lesson DTO through that route. Reuse the current publish toggle and explicit teacher confirmation pattern. Return a mixed publish result that the browser can summarize without inventing a client-only publish contract. Keep save/resume/remove/regenerate preview-first, but let the explicit publish action create or update the matching activity entries. Add regression tests that pin the new handler case, the continued teacher authorization boundary, and the evidence of mixed brownfield targets.</action>
  <acceptance_criteria>
    - `teacher/aiprovider_api.ashx` contains `case "fullLessonPublish":` and keeps `activityPlanPublish` intact for the old single-activity flow
    - `js/courseedit.js` sends the current full-lesson draft to `action=fullLessonPublish` when the teacher confirms publish
    - source tests pin the continued use of the current course editor publish row and explicit confirmation flow
    - the mixed publish path fails closed instead of partially succeeding when a block payload is unsupported or invalid
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter CourseEdit</automated>
    <manual>Authenticated browser UAT: generate or resume a mixed draft with guided inquiry and existing activity types, publish once with the student toggle off, publish again with the toggle on after a draft change, and confirm the same block set updates instead of duplicating entries.</manual>
  </verify>
  <done>Teachers can publish one confirmed mixed full-lesson draft through the normal course editor workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher browser -> `fullLessonPublish` handler | user-controlled mixed draft JSON crosses into privileged publish code and must be revalidated before brownfield writes |
| full-lesson publish core -> brownfield tables | one teacher action fans out into multiple existing entities and must not leave partial or duplicate publish state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | T | mixed full-lesson publish core | mitigate | Revalidate the full-lesson DTO server-side and reject unsupported or malformed blocks before the transaction begins |
| T-10-05 | T | block-level republish behavior | mitigate | Preserve server-owned publish links keyed by `blockKey` so republish updates matching entities instead of blindly inserting duplicates |
| T-10-06 | E | `fullLessonPublish` handler | mitigate | Reuse existing teacher/course authorization and keep the route inside `teacher/aiprovider_api.ashx` rather than exposing a looser endpoint |
</threat_model>

<verification>
- Run `dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter FullLesson`.
- Run `dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter CourseEdit`.
- Manually verify mixed publish and republish behavior in an authenticated teacher session.
</verification>

<success_criteria>
- The teacher can publish one confirmed mixed full-lesson draft from the existing course editor.
- Mixed publish creates or updates the matching set of classroom activity entries in draft order.
- Republish is block-key-aware and does not blindly duplicate brownfield rows.
</success_criteria>

<output>
After completion, create `.planning/phases/10-guided-inquiry-fallback-and-combined-publish/10-guided-inquiry-fallback-and-combined-publish-02-SUMMARY.md`
</output>
