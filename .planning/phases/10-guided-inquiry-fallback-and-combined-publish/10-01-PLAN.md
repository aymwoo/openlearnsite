---
phase: 10-guided-inquiry-fallback-and-combined-publish
plan: "01"
type: execute
wave: 1
depends_on:
  - "09-02"
files_modified:
  - App_Code/Common/AIActivityPlanDraftHelper.cs
  - App_Code/Common/AIActivityPlanSavedDraftHelper.cs
  - teacher/aiprovider_api.ashx
  - teacher/courseedit.aspx
  - js/courseedit.js
  - Tests/CommonLogicTests/CommonLogicTests.cs
  - Tests/TeacherRegressionTests/TeacherRegressionTests.cs
autonomous: true
requirements:
  - INQ-01
  - INQ-02
must_haves:
  truths:
    - Full-lesson drafts can carry a server-owned `guidedInquiry` fallback block beside `quiz`, `resource-study`, and `webCourseware`.
    - Guided inquiry payload validation fails closed when required inquiry goal, prompt, or ordered step data is missing or malformed.
    - Teachers can preview the guided inquiry block in the same mixed full-lesson draft flow without publishing it yet.
  artifacts:
    - path: App_Code/Common/AIActivityPlanDraftHelper.cs
      provides: Full-lesson typed payload contract and validation entry point to extend with `guidedInquiry`.
    - path: App_Code/Common/AIActivityPlanSavedDraftHelper.cs
      provides: Saved-draft round-trip for the server-owned full-lesson DTO including fallback blocks.
    - path: teacher/aiprovider_api.ashx
      provides: Authenticated full-lesson generate and regenerate boundary where guided inquiry fallback becomes available.
    - path: teacher/courseedit.aspx
      provides: Existing mixed full-lesson preview shell that should render inquiry blocks without a new page.
    - path: js/courseedit.js
      provides: Browser rendering for typed block previews and fallback summaries.
    - path: Tests/CommonLogicTests/CommonLogicTests.cs
      provides: Payload-contract and validation coverage for `guidedInquiry`.
    - path: Tests/TeacherRegressionTests/TeacherRegressionTests.cs
      provides: Source-lock coverage for fallback generation wiring and preview rendering.
  key_links:
    - from: teacher/aiprovider_api.ashx
      to: App_Code/Common/AIActivityPlanDraftHelper.cs
      via: full-lesson generate and regenerate flow with typed block validation.
      pattern: fullLessonGenerate|fullLessonRegenerateBlock|guidedInquiry
    - from: teacher/courseedit.aspx
      to: js/courseedit.js
      via: guided inquiry block-card rendering inside the existing preview shell.
      pattern: activity-plan|guidedInquiry|preview
---

<objective>
Add the guided inquiry fallback block to the existing full-lesson draft so mixed
lessons can cover unsupported moments without leaving the current teacher review
workflow.

Purpose: `INQ-01` and the preview part of `INQ-02` depend on a stable typed
fallback contract first. This plan makes `guidedInquiry` a real server-owned
block type that can be generated, validated, saved, resumed, regenerated, and
previewed beside the Phase 9 existing-activity blocks.
Output: guided inquiry payload schema, fail-closed validation, fallback handler
support, recognizable preview rendering, and focused tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-guided-inquiry-fallback-and-combined-publish/10-CONTEXT.md
@.planning/phases/10-guided-inquiry-fallback-and-combined-publish/10-DISCUSSION-LOG.md
@.planning/phases/09-existing-activity-block-composition/09-CONTEXT.md
@App_Code/Common/AIActivityPlanDraftHelper.cs
@App_Code/Common/AIActivityPlanSavedDraftHelper.cs
@teacher/aiprovider_api.ashx
@teacher/courseedit.aspx
@js/courseedit.js
@student/showmission.aspx.cs
@Tests/CommonLogicTests/CommonLogicTests.cs
@Tests/TeacherRegressionTests/TeacherRegressionTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a typed `guidedInquiry` payload contract and fail-closed validation</name>
  <files>App_Code/Common/AIActivityPlanDraftHelper.cs, App_Code/Common/AIActivityPlanSavedDraftHelper.cs, Tests/CommonLogicTests/CommonLogicTests.cs</files>
  <read_first>
    App_Code/Common/AIActivityPlanDraftHelper.cs
    App_Code/Common/AIActivityPlanSavedDraftHelper.cs
    Tests/CommonLogicTests/CommonLogicTests.cs
    student/showmission.aspx.cs
  </read_first>
  <behavior>
    - Test 1: valid `guidedInquiry` blocks require a title, inquiry goal or prompt, and ordered inquiry steps
    - Test 2: malformed or partial `guidedInquiry` payloads are rejected instead of silently surviving inside a mixed full-lesson draft
    - Test 3: saved-draft serialization round-trips `guidedInquiry` payloads under the existing full-lesson block envelope
  </behavior>
  <action>Per D-01, D-02, D-03, and T-10-01, extend the full-lesson typed block contract with the smallest safe `guidedInquiry` payload. Reuse the existing AI mission guidance structure where practical so later publish work can transform the fallback block into the current guided mission runtime without a second inquiry schema. Add focused common-logic tests that pin the required inquiry fields and reject malformed or partial payloads.</action>
  <acceptance_criteria>
    - helper code contains a distinct validation or parsing branch for `guidedInquiry`
    - required inquiry payload fields are pinned in tests and cannot be silently omitted
    - saved-draft serialization preserves `guidedInquiry` content under the existing full-lesson DTO
    - `Tests/CommonLogicTests/CommonLogicTests.cs` contains test names including `GuidedInquiry` or `FullLesson`
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter FullLesson</automated>
  </verify>
  <done>The codebase has a test-protected guided inquiry fallback contract that later publish logic can trust.</done>
</task>

<task type="auto">
  <name>Task 2: Extend full-lesson generate, regenerate, and preview flows to surface guided inquiry fallback blocks</name>
  <files>teacher/aiprovider_api.ashx, teacher/courseedit.aspx, js/courseedit.js, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    teacher/aiprovider_api.ashx
    teacher/courseedit.aspx
    js/courseedit.js
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
  </read_first>
  <action>Per D-08, D-09, and T-10-02, update the authenticated full-lesson generate and block-regenerate flow so the server can return `guidedInquiry` blocks when existing activity types are not a good fit. Keep this work inside the current full-lesson DTO and course editor preview shell. In the browser, render a recognizable inquiry summary that tells the teacher why the fallback block exists and what students will investigate, but keep Phase 10 Plan 01 preview-only: no publish fan-out, no `Mission`/`Exam`/`ListMenu` row creation yet.</action>
  <acceptance_criteria>
    - `teacher/aiprovider_api.ashx` contains evidence of `guidedInquiry` support in full-lesson generate or regenerate paths
    - `js/courseedit.js` contains preview rendering helpers or branches for `guidedInquiry`
    - `teacher/courseedit.aspx` contains only the minimal UI hooks or styles needed for inquiry block previews
    - `Tests/TeacherRegressionTests/TeacherRegressionTests.cs` includes assertions for `guidedInquiry` wiring or preview strings
    - no Plan 01 path creates brownfield publish rows yet
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter CourseEdit</automated>
  </verify>
  <done>Teachers can review a mixed full-lesson draft containing guided inquiry fallback blocks before combined publish exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI/provider response -> full-lesson draft parser | model-produced inquiry payloads cross into privileged server validation and must not survive as partial fallback blocks |
| server-owned inquiry DTO -> browser preview | the teacher-visible inquiry summary must reflect the server payload, not a client-invented fallback interpretation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | `guidedInquiry` payload parser | mitigate | Reject malformed or partial inquiry payloads through dedicated helper validation and unit tests |
| T-10-02 | I | inquiry preview UI | mitigate | Render inquiry summaries from the server DTO only and pin required preview strings in source tests |
| T-10-03 | T | preview-only full-lesson fallback flow | mitigate | Keep generate/save/resume/remove/regenerate scoped to the full-lesson draft and avoid publish fan-out in Plan 01 |
</threat_model>

<verification>
- Run `dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter FullLesson`.
- Run `dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter CourseEdit`.
- Read `teacher/aiprovider_api.ashx` and confirm `guidedInquiry` support lands in full-lesson generate/regenerate only, not publish yet.
</verification>

<success_criteria>
- The server owns a minimal typed `guidedInquiry` fallback payload.
- Guided inquiry preview works inside the same mixed full-lesson draft flow.
- Plan 01 stays preview-first and does not yet create brownfield publish rows.
</success_criteria>

<output>
After completion, create `.planning/phases/10-guided-inquiry-fallback-and-combined-publish/10-guided-inquiry-fallback-and-combined-publish-01-SUMMARY.md`
</output>
