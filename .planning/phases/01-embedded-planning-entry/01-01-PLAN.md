---
phase: 01-embedded-planning-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - App_Code/Common/AIActivityPlanPromptBuilder.cs
  - App_Code/Bll/AIActivityPlanSkillBootstrap.cs
  - teacher/aiprovider_api.ashx
  - Tests/CommonLogicTests/CommonLogicTests.cs
  - Tests/CommonLogicTests/CommonLogicTests.csproj
autonomous: true
requirements:
  - INPUT-04
  - FLOW-03
user_setup: []
must_haves:
  truths:
    - Teacher planning requests reuse the existing authenticated AI provider route.
    - Existing course editor content can be sent as supporting planning context.
    - Typed topic input remains the primary intent when current editor content disagrees.
  artifacts:
    - path: App_Code/Common/AIActivityPlanPromptBuilder.cs
      provides: Server-side prompt composition for the course editor activity-plan assistant.
    - path: teacher/aiprovider_api.ashx
      provides: Dedicated activity-plan request branch on the existing provider handler.
    - path: Tests/CommonLogicTests/CommonLogicTests.cs
      provides: Automated prompt-building and handler-adjacent coverage.
  key_links:
    - from: teacher/aiprovider_api.ashx
      to: App_Code/Common/AIActivityPlanPromptBuilder.cs
      via: activity-plan request composition
    - from: teacher/aiprovider_api.ashx
      to: App_Code/Bll/AIProvider.cs
      via: existing default-provider resolution
---

<objective>
Add the backend contract for the embedded activity-plan assistant on the course
editor.

Purpose: make Phase 1 reuse the existing AI provider and skill infrastructure
while enforcing the topic-first, structured-field request rules from CONTEXT.md.
Output: a server-side prompt builder, scoped planning-skill bootstrap, and a
dedicated activity-plan action on `teacher/aiprovider_api.ashx` with automated
coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-embedded-planning-entry/01-CONTEXT.md
@.planning/phases/01-embedded-planning-entry/01-RESEARCH.md
@teacher/aiprovider_api.ashx
@App_Code/Bll/AIGaugeGenerator.cs
@App_Code/Bll/AIProvider.cs
@App_Code/Bll/AICustomSkill.cs
@App_Code/Model/AICustomSkill.cs
@Tests/CommonLogicTests/CommonLogicTests.cs
@Tests/CommonLogicTests/CommonLogicTests.csproj

<interfaces>
From `App_Code/Model/AICustomSkill.cs`:
```csharp
public string SkillName { get; set; }
public string PromptContent { get; set; }
public string SkillScope { get; set; }
public bool IsActive { get; set; }
```

From `App_Code/Bll/AIProvider.cs`:
```csharp
public LearnSite.Model.AIProvider GetModel(int Id);
public List<LearnSite.Model.AIProvider> GetModelList(string strWhere);
public bool SetDefault(int Id);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scoped activity-plan prompt contracts</name>
  <files>App_Code/Common/AIActivityPlanPromptBuilder.cs, App_Code/Bll/AIActivityPlanSkillBootstrap.cs, Tests/CommonLogicTests/CommonLogicTests.cs, Tests/CommonLogicTests/CommonLogicTests.csproj</files>
  <read_first>App_Code/Bll/AIGaugeGenerator.cs, App_Code/Bll/AICustomSkill.cs, App_Code/Model/AICustomSkill.cs, Tests/CommonLogicTests/CommonLogicTests.cs, Tests/CommonLogicTests/CommonLogicTests.csproj, .planning/phases/01-embedded-planning-entry/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: topic is required and becomes the primary intent string per D-03 and D-07.
    - Test 2: optional grade, duration, and teaching goals are included only when provided per D-05.
    - Test 3: existing editor content is appended as supporting background, not as the primary request body, per D-06 and D-07.
    - Test 4: the default scoped custom skill uses an explicit activity-planning scope string and dedicated assistant prompt per D-08.
  </behavior>
  <action>Create `App_Code/Common/AIActivityPlanPromptBuilder.cs` with a small request contract and a static builder that accepts `topic`, `grade`, `duration`, `teachingGoals`, and `existingCourseContent`, then returns a single provider-ready prompt string. The builder must require a non-empty topic, include the exact optional field labels `授课年级`, `课时/时长`, and `教学目标`, and add existing course content under a supporting-background section that explicitly says the typed topic is the current teacher intent when content conflicts (per D-07). Also add `App_Code/Bll/AIActivityPlanSkillBootstrap.cs` that follows the `AIGaugeGenerator.EnsureDefaultGaugeSkill()` pattern to ensure one active `AICustomSkill` row exists for an activity-planning scope such as `activity_plan_courseedit`, with dedicated assistant wording rather than generic writing-helper wording per D-08. Link any new helper files into `Tests/CommonLogicTests/CommonLogicTests.csproj`, then add focused xUnit coverage in `Tests/CommonLogicTests/CommonLogicTests.cs` for prompt composition and default-skill metadata.</action>
  <verify>
    <automated>dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter "FullyQualifiedName~ActivityPlan"</automated>
  </verify>
  <acceptance_criteria>
    - `App_Code/Common/AIActivityPlanPromptBuilder.cs` contains a builder for `topic`, `grade`, `duration`, `teachingGoals`, and `existingCourseContent`.
    - `App_Code/Common/AIActivityPlanPromptBuilder.cs` contains wording that existing content is `支持背景` or equivalent support context while the topic is the current intent.
    - `App_Code/Bll/AIActivityPlanSkillBootstrap.cs` contains the scope string `activity_plan_courseedit`.
    - `Tests/CommonLogicTests/CommonLogicTests.cs` contains at least one test method name with `ActivityPlan`.
  </acceptance_criteria>
  <done>Prompt-building rules from D-03 through D-08 are encoded server-side, and xUnit coverage proves the topic-first and optional-field behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route activity-plan requests through the existing AI provider handler</name>
  <files>teacher/aiprovider_api.ashx, Tests/CommonLogicTests/CommonLogicTests.cs</files>
  <read_first>teacher/aiprovider_api.ashx, App_Code/Common/AIActivityPlanPromptBuilder.cs, App_Code/Bll/AIActivityPlanSkillBootstrap.cs, App_Code/Bll/AIProvider.cs, .planning/phases/01-embedded-planning-entry/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: empty topic requests are rejected before provider calls.
    - Test 2: valid activity-plan requests reuse the default provider path and skill bootstrap.
    - Test 3: existing course content is passed as supporting background when present.
  </behavior>
  <action>Extend `teacher/aiprovider_api.ashx` with a dedicated `activityPlan` action instead of overloading the existing generic `chat` branch. Read `topic`, `grade`, `duration`, `teachingGoals`, and `existingCourseContent` from the request, trim them, reject empty topic values with `success:false`, call the new skill-bootstrap helper to ensure the dedicated planning skill exists, build the final prompt with `AIActivityPlanPromptBuilder`, and then send that server-composed prompt through the same default-provider resolution and `/chat/completions` call path already used by `Chat`. Refactor only enough shared code to avoid duplicating provider selection and HTTP request logic; do not introduce a new handler, a new provider client, or an editable raw prompt parameter. Add or extend xUnit tests in `Tests/CommonLogicTests/CommonLogicTests.cs` to cover request validation and prompt composition helpers that the handler now relies on.</action>
  <verify>
    <automated>dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter "FullyQualifiedName~ActivityPlan"</automated>
  </verify>
  <acceptance_criteria>
    - `teacher/aiprovider_api.ashx` contains `case "activityPlan":`.
    - `teacher/aiprovider_api.ashx` reads `topic`, `grade`, `duration`, `teachingGoals`, and `existingCourseContent`.
    - `teacher/aiprovider_api.ashx` calls `AIActivityPlanPromptBuilder` and the new skill bootstrap helper.
    - `teacher/aiprovider_api.ashx` does not introduce a second provider endpoint path beyond the existing OpenAI-compatible `/chat/completions` flow.
  </acceptance_criteria>
  <done>The course-editor assistant has a validated backend request contract that reuses the current authenticated AI provider infrastructure per FLOW-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `teacher/aiprovider_api.ashx` | Untrusted teacher-entered topic, goals, duration, and existing editor content enter the server handler |
| `teacher/aiprovider_api.ashx` → AI provider | Internal lesson content leaves the app and is sent to the configured provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `teacher/aiprovider_api.ashx` activity-plan action | mitigate | Validate non-empty topic, trim/bound optional fields, and compose the provider prompt server-side rather than trusting a browser-supplied final prompt |
| T-01-02 | I | prompt builder and provider call | mitigate | Mark existing course content as supporting background, limit included content length, and send it only through the authenticated default-provider path |
| T-01-03 | D | provider handler | mitigate | Reject oversized/empty requests early and keep the existing request timeout behavior instead of adding an unbounded planning loop |
| T-01-04 | E | handler authentication | accept | Reuse the existing teacher-cookie gate in `aiprovider_api.ashx`; no new unauthenticated route is added in this phase |
</threat_model>

<verification>
- [ ] `dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter "FullyQualifiedName~ActivityPlan"`
- [ ] Read `teacher/aiprovider_api.ashx` and confirm the `activityPlan` branch still uses the existing default-provider flow
- [ ] Confirm the prompt builder encodes D-06 and D-07 topic-vs-context precedence rules
</verification>

<success_criteria>
- All task tests pass.
- The handler exposes a dedicated course-editor planning action without adding a new AI stack.
- Prompt composition lives on the server and uses structured fields plus supporting editor context.
</success_criteria>

<output>
After completion, create `.planning/phases/01-embedded-planning-entry/01-01-SUMMARY.md`
</output>
