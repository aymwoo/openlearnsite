---
phase: 03-guided-review-and-section-regeneration
plan: 02
type: execute
wave: 2
depends_on:
  - 02-02
  - 03-01
files_modified:
  - teacher/courseedit.aspx
  - js/courseedit.js
  - Tests/TeacherRegressionTests/TeacherRegressionTests.cs
autonomous: true
requirements:
  - EDIT-01
  - EDIT-02
user_setup: []
must_haves:
  truths:
    - The teacher can review the full generated draft before changing any lesson editor content.
    - Each top-level preview section has its own regenerate action, and non-targeted sections stay visible while one section is retrying.
    - A successful section retry updates the preview with the merged full draft from the server.
    - A failed section retry leaves the last valid preview visible and localizes the error to the targeted section card.
  artifacts:
    - path: teacher/courseedit.aspx
      provides: Stable review-panel host markup for section-level retry controls.
    - path: js/courseedit.js
      provides: Section-card action rendering, section-local busy/error state, and merged-draft rerender behavior.
    - path: Tests/TeacherRegressionTests/TeacherRegressionTests.cs
      provides: Source-level regression locks for section-regeneration UI hooks and preview-preserving failure behavior.
  key_links:
    - from: teacher/courseedit.aspx
      to: js/courseedit.js
      via: section-card IDs, result container, and regenerate-button hooks
    - from: js/courseedit.js
      to: teacher/aiprovider_api.ashx
      via: `action=activityPlanRegenerateSection` XHR calls
    - from: js/courseedit.js
      to: `lastActivityPlanDraftResponse` state
      via: merged full-draft rerender and preview-preserving failure handling
---

<objective>
Add guided section review controls to the existing course-editor preview panel.

Purpose: let teachers improve weak sections without losing strong sections or
touching the lesson editor until they choose to do so in a later phase.
Output: section-level regenerate controls, section-local request state, and
regression coverage for preview-safe review behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-guided-review-and-section-regeneration/03-CONTEXT.md
@.planning/phases/03-guided-review-and-section-regeneration/03-RESEARCH.md
@.planning/phases/02-structured-plan-draft-generation/02-02-SUMMARY.md
@teacher/courseedit.aspx
@js/courseedit.js
@teacher/aiprovider_api.ashx
@Tests/TeacherRegressionTests/TeacherRegressionTests.cs

<interfaces>
From `teacher/courseedit.aspx`:
```html
<div id="activity-plan-result" class="ai-result-area" aria-live="polite"></div>
<button type="button" class="ai-action-btn" onclick="copyActivityPlanResult()">
```

From `js/courseedit.js`:
```javascript
var lastActivityPlanDraftResponse = null;
function renderActivityPlanDraft(responseData)
function renderActivityPlanListCard(container, title, items)
function renderActivityPlanStepsCard(container, steps)
function buildActivityPlanCopyText()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add section-level regenerate controls to the existing preview cards</name>
  <files>teacher/courseedit.aspx, js/courseedit.js, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>teacher/courseedit.aspx, js/courseedit.js, Tests/TeacherRegressionTests/TeacherRegressionTests.cs, .planning/phases/03-guided-review-and-section-regeneration/03-CONTEXT.md, .planning/phases/02-structured-plan-draft-generation/02-02-SUMMARY.md</read_first>
  <behavior>
    - Test 1: the review panel still shows the full preview before any lesson editor content changes.
    - Test 2: each top-level section card exposes one regenerate action, including one card-level action for `activitySteps` rather than per-step buttons.
    - Test 3: the review controls do not add accept/reject/lock state.
    - Test 4: the preview remains copyable and read-only after control wiring changes.
  </behavior>
  <action>Per D-01, D-02, D-05, and D-06, keep `teacher/courseedit.aspx` as the only review host and extend `js/courseedit.js` so the five top-level preview sections render with section-level regenerate affordances only. Add one action for `teachingGoals`, one for the whole `activitySteps` card, one for `resources`, one for `assessment`, and one for `teacherReminder`. Use concrete section keys matching the server allowlist: `teachingGoals`, `activitySteps`, `resources`, `assessment`, and `teacherReminder`. Do not add per-step retry buttons, full-draft approval state, accept/reject toggles, or any write-back into `mcontent`. Keep the existing preview metadata, section order, and copy/export path intact so the teacher still reviews a complete preview-only draft before later apply work in Phase 4.</action>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --framework net8.0 --filter "FullyQualifiedName~CourseEdit"</automated>
  </verify>
  <acceptance_criteria>
    - `js/courseedit.js` contains the exact section keys `teachingGoals`, `activitySteps`, `resources`, `assessment`, and `teacherReminder` in the preview-control render path.
    - `js/courseedit.js` contains a section-level regenerate handler and does not contain per-step regenerate button creation inside the step loop.
    - `teacher/courseedit.aspx` still contains `id="activity-plan-result"`, `生成活动计划`, and `复制草案`.
    - `Tests/TeacherRegressionTests/TeacherRegressionTests.cs` contains assertions for section-regeneration control hooks.
  </acceptance_criteria>
  <done>The course editor preview now exposes guided section-level retry controls without adding any approval-state subsystem or touching lesson editor content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep retry state local to the targeted section and preserve the last valid draft on failure</name>
  <files>js/courseedit.js, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>js/courseedit.js, teacher/aiprovider_api.ashx, Tests/TeacherRegressionTests/TeacherRegressionTests.cs, .planning/phases/03-guided-review-and-section-regeneration/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: retry requests post `action=activityPlanRegenerateSection` with the current draft and targeted section key.
    - Test 2: while one section is retrying, only that section shows busy state and the rest of the preview remains visible.
    - Test 3: a successful retry replaces `lastActivityPlanDraftResponse` with the merged full draft returned by the server.
    - Test 4: a failed retry keeps the prior valid preview on screen and shows only section-local error feedback.
  </behavior>
  <action>Per D-07, D-08, and D-09, add a section-local retry state model in `js/courseedit.js` keyed by the five allowed section names. Implement a concrete request path named `activityPlanRegenerateSection` that posts `topic`, `grade`, `duration`, `teachingGoals`, `existingCourseContent`, `sectionTarget`, and the current `lastActivityPlanDraftResponse.draft` JSON to `teacher/aiprovider_api.ashx`. The targeted section card must show its own loading text or disabled retry button while the request is in flight, but the rest of the preview must stay rendered and unchanged. On success, replace `lastActivityPlanDraftResponse` with the returned merged full draft and rerender the full preview. On parse failure, timeout, network failure, or `success:false`, keep `lastActivityPlanDraftResponse` unchanged, keep the existing full preview visible, and surface the failure only inside the targeted section card or its local status message.</action>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --framework net8.0 --filter "FullyQualifiedName~CourseEdit"</automated>
  </verify>
  <acceptance_criteria>
    - `js/courseedit.js` contains `action=activityPlanRegenerateSection`.
    - `js/courseedit.js` posts `sectionTarget` and the current draft JSON in the section-regeneration request.
    - `js/courseedit.js` keeps `lastActivityPlanDraftResponse` unchanged on failure paths and updates it only after a successful merged response.
    - `Tests/TeacherRegressionTests/TeacherRegressionTests.cs` contains assertions for local section-state hooks and preview-preserving failure behavior.
  </acceptance_criteria>
  <done>The guided review loop now retries one section at a time, keeps the rest of the preview stable, and never discards the last valid full draft on a failed retry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merged draft JSON → browser renderer | Server-returned content enters the teacher preview and must remain safe text-only UI |
| section-local retry state → full preview state | One section retry must not wipe or mutate other visible sections on failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | I | section preview rendering | mitigate | Keep DOM creation plus `textContent` rendering for all merged-draft values and status messages |
| T-03-06 | T | local retry state | mitigate | Scope loading and error flags by section key so one retry cannot clear the full preview state |
| T-03-07 | R | teacher review workflow | mitigate | Keep preview-only messaging and never write merged draft content into `mcontent` during Phase 3 |
</threat_model>

<verification>
- [ ] `dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --framework net8.0 --filter "FullyQualifiedName~CourseEdit"`
- [ ] Read `js/courseedit.js` and confirm section retry requests use `action=activityPlanRegenerateSection`
- [ ] Read `js/courseedit.js` and confirm failure paths keep the previous `lastActivityPlanDraftResponse`
- [ ] Confirm no section-regeneration success path writes merged draft content into `mcontent` or editor instances
</verification>

<success_criteria>
- Focused CourseEdit regression coverage passes.
- Teachers can retry any one allowed top-level section from the existing preview panel.
- Non-targeted sections stay visible during retries.
- Failed retries preserve the last valid full preview and keep the lesson editor untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/03-guided-review-and-section-regeneration/03-02-SUMMARY.md`
</output>
