---
phase: 11-composed-runtime-and-progress-visibility
plan: "02"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - student/Scm.master.cs
  - student/showmission.aspx.cs
  - student/myinfo.aspx.cs
  - teacher/courseshow.aspx.cs
  - App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs
  - Tests/TeacherRegressionTests/TeacherRegressionTests.cs
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - RUN-03
  - RUN-04
must_haves:
  truths:
    - Students can enter each published composed block through the existing classroom navigation flow and see block context that matches its full-lesson position.
    - Each supported block type continues to open in its brownfield runtime page, with minimal composed-lesson context or progress cues added where useful.
    - Teacher and student completion views can distinguish per-block progress across the composed lesson after publication.
  artifacts:
    - path: student/Scm.master.cs
      provides: Existing student classroom navigation and finish-state icons.
    - path: student/showmission.aspx.cs
      provides: Guided mission runtime reused by composed `guidedInquiry` blocks.
    - path: student/myinfo.aspx.cs
      provides: Student summary surface for completed course/activity visibility.
    - path: teacher/courseshow.aspx.cs
      provides: Teacher course display surface after mixed publish.
    - path: App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs
      provides: Ordered composed block runtime/progress summaries for page consumption.
    - path: Tests/TeacherRegressionTests/TeacherRegressionTests.cs
      provides: Source-lock coverage for runtime routing and progress visibility wiring.
  key_links:
    - from: student/Scm.master.cs
      to: App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs
      via: composed block order/progress cues layered onto current menu items.
      pattern: blockKey|ListMenuId|completion
    - from: teacher/courseshow.aspx.cs
      to: App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs
      via: teacher-visible per-block composed publish/progress summary.
      pattern: publishLinks|PublishedBlocks|progress
---

<objective>
Wire the composed runtime/progress contract into the current student and teacher
surfaces so mixed full-lesson blocks behave like one trackable lesson after
publish.

Purpose: `RUN-01` through `RUN-04` are satisfied only when students and teachers
can use and understand composed block runtime/progress inside the existing
brownfield flow. This plan lands the visible page wiring.
Output: student menu/runtime context updates, student completion summary updates,
teacher composed progress visibility, and regression coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-composed-runtime-and-progress-visibility/11-CONTEXT.md
@.planning/phases/11-composed-runtime-and-progress-visibility/11-DISCUSSION-LOG.md
@App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs
@student/Scm.master.cs
@student/showmission.aspx.cs
@student/description.aspx.cs
@student/ware.aspx.cs
@webform/preview.aspx.cs
@student/myinfo.aspx.cs
@teacher/courseshow.aspx.cs
@Tests/TeacherRegressionTests/TeacherRegressionTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add composed block order and progress cues to the existing student flow</name>
  <files>student/Scm.master.cs, student/showmission.aspx.cs, student/myinfo.aspx.cs, App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    student/Scm.master.cs
    student/showmission.aspx.cs
    student/myinfo.aspx.cs
    App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
  </read_first>
  <action>Per D-01 through D-05 and T-11-04, update the current student navigation and summary surfaces to show that published mixed blocks belong to one composed lesson and what each block's completion state is. Reuse the existing `ListMenu` navigation and finish-state visuals, but add the smallest clear cue for block order, block type, or composed progress where it helps orientation. Keep each block opening in its current type-specific page, and add minimal composed context to mission runtime pages when needed so guided inquiry or resource-study blocks do not feel detached from the full lesson.</action>
  <acceptance_criteria>
    - `student/Scm.master.cs` continues to route by current `Ltype` values and also contains evidence of composed block order or progress handling
    - `student/showmission.aspx.cs` keeps existing mission upload/runtime restrictions while adding only minimal composed lesson context if needed
    - `student/myinfo.aspx.cs` can distinguish composed lesson block progress from generic course completion aggregation where Phase 11 requires it
    - no new AI-only student runtime page is introduced
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter ComposedRuntime</automated>
    <manual>Authenticated browser UAT: open a mixed published lesson as a student, enter each block from the normal menu, complete at least one mission-backed block and one non-mission block, then confirm progress cues change across the composed block set.</manual>
  </verify>
  <done>Students can move through the published composed lesson in the normal classroom flow and see which blocks are done or still pending.</done>
</task>

<task type="auto">
  <name>Task 2: Add teacher-facing composed block progress visibility on existing course surfaces</name>
  <files>teacher/courseshow.aspx.cs, App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    teacher/courseshow.aspx.cs
    App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
  </read_first>
  <action>Per D-05, D-06, D-09, and T-11-05, add the smallest teacher-visible summary that shows which brownfield activities belong to the composed lesson and how block-level progress differs across them after publish. Reuse `teacher/courseshow.aspx.cs` and its current menu display rather than creating a new dashboard. If needed, surface block order/type/progress hints beside existing menu items or in a compact summary area so teachers can distinguish the composed lesson block set from unrelated legacy menu items.</action>
  <acceptance_criteria>
    - teacher course display continues to use the current course/menu surface and adds only minimal composed progress visibility
    - the UI can distinguish multiple published blocks from one composed lesson rather than treating them as unrelated menu rows
    - source tests pin the presence of composed progress logic and the absence of a new dashboard route
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter CourseShow</automated>
    <manual>Authenticated teacher UAT: open a course containing a mixed published lesson, confirm the composed block set is recognizable, and verify block-level progress changes after student completion activity.</manual>
  </verify>
  <done>Teachers can tell how each published composed block is progressing without leaving the normal course display flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composed runtime helper -> student navigation/runtime pages | summary metadata affects visible orientation and completion cues during classroom execution |
| composed runtime helper -> teacher course display | teacher-visible progress summaries can influence classroom decisions and must stay tied to real brownfield evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | I | student composed progress cues | mitigate | Render composed block order/progress from server-owned helper output only and preserve existing brownfield route semantics |
| T-11-05 | I | teacher composed course visibility | mitigate | Show block-level summaries tied to published ids and existing completion evidence, not inferred title matches |
| T-11-06 | T | runtime page regressions | mitigate | Keep each activity type on its current runtime page and source-test the continued `Ltype` routing contract |
</threat_model>

<verification>
- Run `dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter ComposedRuntime`.
- Run `dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter CourseShow`.
- Manually verify student entry/completion and teacher course visibility with an authenticated mixed published lesson.
</verification>

<success_criteria>
- Students can enter and run each published composed block through the normal classroom navigation.
- Runtime pages remain type-correct while exposing minimal composed lesson context/progress.
- Teacher and student surfaces can distinguish block-level progress across the composed lesson.
</success_criteria>

<output>
After completion, create `.planning/phases/11-composed-runtime-and-progress-visibility/11-composed-runtime-and-progress-visibility-02-SUMMARY.md`
</output>
