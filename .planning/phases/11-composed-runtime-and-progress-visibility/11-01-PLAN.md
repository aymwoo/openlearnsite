---
phase: 11-composed-runtime-and-progress-visibility
plan: "01"
type: execute
wave: 1
depends_on:
  - "10-02"
files_modified:
  - App_Code/Common/AIActivityPlanSavedDraftHelper.cs
  - App_Code/Common/AIActivityPlanDraftHelper.cs
  - App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs
  - App_Code/Model/AIActivityPlanPublishResult.cs
  - Tests/CommonLogicTests/CommonLogicTests.cs
  - Tests/TeacherRegressionTests/TeacherRegressionTests.cs
autonomous: true
requirements:
  - RUN-01
  - RUN-04
must_haves:
  truths:
    - The server can resolve one published full-lesson draft into ordered composed block runtime summaries keyed by `blockKey`.
    - Each composed block summary includes the brownfield runtime target and enough metadata to determine a fail-closed completion state.
    - Missing or malformed publish links do not silently produce misleading composed progress results.
  artifacts:
    - path: App_Code/Common/AIActivityPlanSavedDraftHelper.cs
      provides: Saved full-lesson draft and `publishLinks` parsing boundary.
    - path: App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs
      provides: New server-owned runtime/progress summary helper for composed published blocks.
    - path: App_Code/Model/AIActivityPlanPublishResult.cs
      provides: Existing published-block result model that may need minimal summary metadata alignment.
    - path: Tests/CommonLogicTests/CommonLogicTests.cs
      provides: Helper and fail-closed progress summary coverage.
    - path: Tests/TeacherRegressionTests/TeacherRegressionTests.cs
      provides: Source-lock coverage that the new helper is consumed by current student/teacher surfaces instead of a new runtime route.
  key_links:
    - from: App_Code/Common/AIActivityPlanSavedDraftHelper.cs
      to: App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs
      via: parsed `publishLinks` and full-lesson block payloads.
      pattern: publishLinks|blockKey|FullLessonDraft
    - from: App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs
      to: App_Code/Bll/MenuWorks.cs
      via: block-level completion lookup through existing brownfield evidence.
      pattern: MenuWorks|GetModelme|completion
---

<objective>
Create the server-owned composed runtime/progress contract that Phase 11 pages can
consume without guessing relationships between full-lesson blocks and brownfield
activities.

Purpose: `RUN-01` and the data foundation of `RUN-04` depend on a stable helper
that can resolve published full-lesson blocks into ordered runtime targets and
completion summaries. This plan lands that helper before page wiring.
Output: composed runtime helper, minimal supporting model updates, fail-closed
publish-link resolution, and focused tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-composed-runtime-and-progress-visibility/11-CONTEXT.md
@.planning/phases/11-composed-runtime-and-progress-visibility/11-DISCUSSION-LOG.md
@App_Code/Common/AIActivityPlanSavedDraftHelper.cs
@App_Code/Common/AIActivityPlanDraftHelper.cs
@App_Code/Dal/AIActivityPlanPublisher.cs
@App_Code/Model/AIActivityPlanPublishResult.cs
@App_Code/Bll/MenuWorks.cs
@App_Code/Bll/Works.cs
@student/Scm.master.cs
@student/myinfo.aspx.cs
@teacher/courseshow.aspx.cs
@Tests/CommonLogicTests/CommonLogicTests.cs
@Tests/TeacherRegressionTests/TeacherRegressionTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a fail-closed composed runtime/progress helper over full-lesson publish links</name>
  <files>App_Code/Common/AIActivityPlanComposedRuntimeHelper.cs, App_Code/Common/AIActivityPlanSavedDraftHelper.cs, App_Code/Common/AIActivityPlanDraftHelper.cs, App_Code/Model/AIActivityPlanPublishResult.cs, Tests/CommonLogicTests/CommonLogicTests.cs</files>
  <read_first>
    App_Code/Common/AIActivityPlanSavedDraftHelper.cs
    App_Code/Common/AIActivityPlanDraftHelper.cs
    App_Code/Dal/AIActivityPlanPublisher.cs
    App_Code/Model/AIActivityPlanPublishResult.cs
    Tests/CommonLogicTests/CommonLogicTests.cs
  </read_first>
  <behavior>
    - Test 1: a valid mixed full-lesson draft with `publishLinks` resolves into ordered composed runtime summaries that keep `blockKey`, `blockType`, title, `ListMenuId`, and runtime route type
    - Test 2: completion lookup uses existing legacy evidence and reports fail-closed status when linked ids or publish metadata are missing
    - Test 3: unsupported or drifted publish-link payloads are rejected instead of producing partial composed progress summaries
  </behavior>
  <action>Per D-03, D-04, D-07, D-08, and T-11-01, add the smallest safe helper that takes a saved full-lesson draft plus current brownfield evidence and returns ordered composed block runtime summaries. Prefer explicit runtime target resolution by block type and published ids over title matching. Keep completion grounded in current models like `MenuWorks`, and use explicit unknown/incomplete states when publish links or block payloads are not trustworthy. Add focused common-logic tests that pin ordering, target resolution, and fail-closed behavior.</action>
  <acceptance_criteria>
    - helper code contains an explicit composed full-lesson runtime/progress resolution path
    - runtime summaries are keyed by `blockKey` and do not depend on title matching
    - tests pin fail-closed handling for missing `publishLinks`, missing ids, or unsupported block types
    - `Tests/CommonLogicTests/CommonLogicTests.cs` contains test names including `ComposedRuntime`, `FullLessonPublish`, or `Progress`
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter ComposedRuntime</automated>
  </verify>
  <done>The server can describe one published composed lesson as ordered runtime/progress blocks that later pages can render safely.</done>
</task>

<task type="auto">
  <name>Task 2: Pin current page integration boundaries around the composed runtime helper</name>
  <files>Tests/TeacherRegressionTests/TeacherRegressionTests.cs, student/Scm.master.cs, student/myinfo.aspx.cs, teacher/courseshow.aspx.cs</files>
  <read_first>
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
    student/Scm.master.cs
    student/myinfo.aspx.cs
    teacher/courseshow.aspx.cs
  </read_first>
  <action>Per D-01, D-02, and D-10, add or update source-lock tests that pin Phase 11's chosen integration boundary before deeper UI wiring. The tests should document that composed runtime/progress stays attached to the current student menu, student summary, and teacher course display surfaces, and does not introduce a new AI-specific student runtime route. Where useful, prepare small page-level hooks or placeholders that Plan 02 can consume, but keep this plan primarily on the helper contract and integration boundary.</action>
  <acceptance_criteria>
    - source tests document continued use of `student/Scm.master.cs`, `student/myinfo.aspx.cs`, and `teacher/courseshow.aspx.cs`
    - source tests reject introduction of a new AI-only student runtime route for composed lessons
    - any page hook added in this plan is minimal and clearly preparatory for Plan 02
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter ComposedRuntime</automated>
  </verify>
  <done>The codebase has a pinned boundary for where composed runtime/progress visibility belongs before page wiring begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved full-lesson draft JSON -> composed runtime helper | persisted publish metadata becomes the source of truth for block runtime and progress summaries |
| composed runtime helper -> student/teacher pages | helper output drives visible progress/UI decisions and must not misreport completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T | composed publish-link resolver | mitigate | Reject missing or drifted `publishLinks` / block payload combinations through explicit validation and unit tests |
| T-11-02 | I | block-level progress summary | mitigate | Ground completion state in current brownfield evidence only and use fail-closed unknown/incomplete states when evidence is missing |
| T-11-03 | S | runtime target resolution | mitigate | Resolve target route from server-owned block type and linked ids rather than client- or title-derived guesses |
</threat_model>

<verification>
- Run `dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter ComposedRuntime`.
- Run `dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter ComposedRuntime`.
- Read page and helper code to confirm the new contract stays attached to current student/teacher surfaces, not a new runtime route.
</verification>

<success_criteria>
- The server owns an ordered composed runtime/progress summary contract for published full lessons.
- Runtime targets and completion states are resolved from stable brownfield ids and current evidence.
- Missing or malformed publish metadata fails closed instead of showing misleading progress.
</success_criteria>

<output>
After completion, create `.planning/phases/11-composed-runtime-and-progress-visibility/11-composed-runtime-and-progress-visibility-01-SUMMARY.md`
</output>
