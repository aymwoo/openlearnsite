---
phase: 05-teacher-activity-publish-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - App_Code/Model/AIActivityPlanPublishRequest.cs
  - App_Code/Model/AIActivityPlanPublishResult.cs
  - App_Code/Model/CourseActivityPlanDraft.cs
  - App_Code/Common/AIActivityPlanSavedDraftHelper.cs
  - App_Code/Dal/CourseActivityPlanDraft.cs
  - App_Code/Utility/UpdateGrade.cs
  - Tests/CommonLogicTests/CommonLogicTests.cs
autonomous: true
requirements:
  - TAP-01
  - TAP-02
must_haves:
  truths:
    - Teacher publish data has a stable server-side contract.
    - One course can resolve back to one linked AI activity identity.
    - Re-publish foundation exists before any UI publish button is wired.
  artifacts:
    - path: App_Code/Model/AIActivityPlanPublishRequest.cs
      provides: Typed publish input contract.
    - path: App_Code/Model/AIActivityPlanPublishResult.cs
      provides: Typed publish result contract.
    - path: App_Code/Model/CourseActivityPlanDraft.cs
      provides: Linked mission/menu identifiers persisted with the draft.
    - path: App_Code/Utility/UpdateGrade.cs
      provides: Upgrade path for new draft-link columns.
  key_links:
    - from: App_Code/Common/AIActivityPlanSavedDraftHelper.cs
      to: App_Code/Model/CourseActivityPlanDraft.cs
      via: BuildRecord and ParseRecord round-trip.
      pattern: LinkedMissionId|LinkedListMenuId
    - from: App_Code/Dal/CourseActivityPlanDraft.cs
      to: CourseActivityPlanDraft table
      via: insert/update column mapping.
      pattern: LinkedMissionId|LinkedListMenuId
---

<objective>
Lock in the publish contracts and one-course linkage foundation before any UI
or handler wiring starts.

Purpose: D-01, D-02, D-03, and D-10 require a stable backend shape so later
publish actions update one linked activity instead of creating duplicates or
falling back to legacy defaults.
Output: typed publish request/result files, draft-link persistence, migration
coverage, and logic tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-teacher-activity-publish-foundation/05-CONTEXT.md
@.planning/phases/05-teacher-activity-publish-foundation/05-RESEARCH.md
@teacher/aiprovider_api.ashx
@App_Code/Model/CourseActivityPlanDraft.cs
@App_Code/Common/AIActivityPlanSavedDraftHelper.cs
@App_Code/Dal/CourseActivityPlanDraft.cs
@App_Code/Utility/UpdateGrade.cs
@Tests/CommonLogicTests/CommonLogicTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define publish contracts for the server-owned flow</name>
  <files>App_Code/Model/AIActivityPlanPublishRequest.cs, App_Code/Model/AIActivityPlanPublishResult.cs, Tests/CommonLogicTests/CommonLogicTests.cs</files>
  <read_first>
    App_Code/Model/CourseActivityPlanDraft.cs
    App_Code/Common/AIActivityPlanSavedDraftHelper.cs
    teacher/aiprovider_api.ashx
    Tests/CommonLogicTests/CommonLogicTests.cs
  </read_first>
  <action>Per D-07 and D-04, add typed publish contracts instead of passing anonymous blobs through the new publish path. Create `AIActivityPlanPublishRequest` with exact properties `Cid`, `Hid`, `Topic`, `PublishToStudents`, `SelectedSectionKeys`, and `Draft`. Create `AIActivityPlanPublishResult` with exact properties `MissionId`, `ListMenuId`, `MissionTitle`, `PublishedToStudents`, and `UpdatedCourseContent`. Add CommonLogic tests that assert the source files contain those property names and reject the legacy pattern of relying on `CheckPublish.Checked=True` from `teacher/missionadd.aspx.cs` for the AI flow (D-10).</action>
  <acceptance_criteria>
    - `App_Code/Model/AIActivityPlanPublishRequest.cs` contains `public int Cid`, `public int Hid`, `public bool PublishToStudents`, `public List<string> SelectedSectionKeys`, and `public LearnSite.Common.ActivityPlanDraft Draft`
    - `App_Code/Model/AIActivityPlanPublishResult.cs` contains `public int MissionId`, `public int ListMenuId`, `public string MissionTitle`, `public bool PublishedToStudents`, and `public string UpdatedCourseContent`
    - `Tests/CommonLogicTests/CommonLogicTests.cs` contains at least one test name including `ActivityPlanPublishRequest`
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter "FullyQualifiedName~ActivityPlanPublishRequest"</automated>
  </verify>
  <done>The publish core has explicit contracts that later plans can call without rediscovering payload shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist linked mission and menu ids on the existing draft record</name>
  <files>App_Code/Model/CourseActivityPlanDraft.cs, App_Code/Common/AIActivityPlanSavedDraftHelper.cs, App_Code/Dal/CourseActivityPlanDraft.cs, App_Code/Utility/UpdateGrade.cs, Tests/CommonLogicTests/CommonLogicTests.cs</files>
  <read_first>
    App_Code/Model/CourseActivityPlanDraft.cs
    App_Code/Common/AIActivityPlanSavedDraftHelper.cs
    App_Code/Dal/CourseActivityPlanDraft.cs
    App_Code/Utility/UpdateGrade.cs
    Tests/CommonLogicTests/CommonLogicTests.cs
  </read_first>
  <behavior>
    - Test 1: draft migration source includes nullable `LinkedMissionId` and `LinkedListMenuId` columns plus upgrade guards
    - Test 2: saved-draft helper round-trips linked ids when the record belongs to the expected course and teacher
    - Test 3: DAL source maps the new columns in both select and upsert paths
  </behavior>
  <action>Per D-01 and D-02, extend the one-record-per-course `CourseActivityPlanDraft` model with nullable `LinkedMissionId` and `LinkedListMenuId` properties. Update `AIActivityPlanSavedDraftHelper` so `BuildRecord(...)` can preserve previously linked ids and `ParseRecord(...)` surfaces them back to callers instead of dropping them. Update `CourseActivityPlanDraft` DAL select and upsert SQL to read/write both columns. Extend `UpdateGrade.UpdateTable1913()` to add the two nullable int columns when the table already exists in upgraded databases. Add CommonLogic tests that grep the migration source, helper source, and DAL source for the exact new column names.</action>
  <acceptance_criteria>
    - `App_Code/Model/CourseActivityPlanDraft.cs` contains `public int? LinkedMissionId` and `public int? LinkedListMenuId`
    - `App_Code/Dal/CourseActivityPlanDraft.cs` contains `LinkedMissionId` and `LinkedListMenuId` in both `update CourseActivityPlanDraft set` and `insert into CourseActivityPlanDraft`
    - `App_Code/Utility/UpdateGrade.cs` contains `LinkedMissionId` and `LinkedListMenuId`
    - `Tests/CommonLogicTests/CommonLogicTests.cs` contains test names including `ActivityPlanDraftMigration` and `ActivityPlanSavedDraft`
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter "FullyQualifiedName~ActivityPlanDraftMigration|FullyQualifiedName~ActivityPlanSavedDraft"</automated>
  </verify>
  <done>The draft row can now anchor the one-linked-AI-activity-per-course rule for later publish and re-publish calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher browser → publish handler | Untrusted publish payload crosses from the browser into authenticated server code |
| publish handler → draft persistence | Linked activity identity is stored and reused across later requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | CourseActivityPlanDraft linkage columns | mitigate | Persist `LinkedMissionId` and `LinkedListMenuId` only through server-owned code paths and cover them with source-level tests |
| T-05-02 | I | Publish request contract | mitigate | Use explicit request/result models so later handlers validate known fields instead of anonymous form keys |
| T-05-03 | R | Upgrade path | mitigate | Add migration-source tests for the new columns so existing deployments cannot silently miss linkage persistence |
</threat_model>

<verification>
- Run the targeted CommonLogic filters for publish-contract and draft-linkage tests.
- Read the updated draft model, DAL, and migration source to confirm the two new
  linkage columns exist everywhere they need to.
</verification>

<success_criteria>
- Publish contracts exist as concrete C# model files.
- The draft table model, DAL, and migration all carry `LinkedMissionId` and
  `LinkedListMenuId`.
- CommonLogic tests pin the linkage contract before the publish core is built.
</success_criteria>

<output>
After completion, create `.planning/phases/05-teacher-activity-publish-foundation/05-teacher-activity-publish-foundation-01-SUMMARY.md`
</output>
