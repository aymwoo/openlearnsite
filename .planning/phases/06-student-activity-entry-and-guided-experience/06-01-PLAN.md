---
phase: 06-student-activity-entry-and-guided-experience
plan: "01"
type: execute
wave: 1
depends_on:
  - "05-03"
files_modified:
  - student/Scm.master.cs
  - student/showmission.aspx
  - student/showmission.aspx.cs
  - Tests/TeacherRegressionTests/TeacherRegressionTests.cs
autonomous: true
requirements:
  - SAE-01
  - SAE-02
must_haves:
  truths:
    - Published AI activities reuse the existing student menu and mission route.
    - Students only reach the AI activity through the existing shown menu contract.
    - The mission page still exposes the existing upload shell while improving guidance readability.
  artifacts:
    - path: student/Scm.master.cs
      provides: Locked menu route from `Ltype=1` to `showmission.aspx`.
    - path: student/showmission.aspx
      provides: Student-facing guided activity presentation shell.
    - path: student/showmission.aspx.cs
      provides: Server-side mission load and AI guidance shaping entry point.
    - path: Tests/TeacherRegressionTests/TeacherRegressionTests.cs
      provides: Source regression coverage for menu and mission-page wiring.
  key_links:
    - from: App_Code/Dal/AIActivityPlanPublisher.cs
      to: student/Scm.master.cs
      via: `Ltype=1` route contract.
      pattern: Ltype=1|showmission.aspx
    - from: student/Scm.master.cs
      to: student/showmission.aspx.cs
      via: `lid` querystring student open flow.
      pattern: showmission.aspx?lid=
---

<objective>
Lock the Phase 5 publish output into the real student menu and page-open flow.

Purpose: The milestone audit shows the teacher publish -> student menu ->
student activity open path is still unverified. Before refining the guidance UI,
Phase 6 must first make that route explicit and testable.
Output: route-safe student entry wiring, mission-page shell updates, and source
regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-v1.1-MILESTONE-AUDIT.md
@.planning/phases/06-student-activity-entry-and-guided-experience/06-CONTEXT.md
@.planning/phases/06-student-activity-entry-and-guided-experience/06-RESEARCH.md
@App_Code/Dal/AIActivityPlanPublisher.cs
@student/Scm.master.cs
@student/showmission.aspx
@student/showmission.aspx.cs
@Tests/TeacherRegressionTests/TeacherRegressionTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the published AI activity route into the student menu flow</name>
  <files>student/Scm.master.cs, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    App_Code/Dal/AIActivityPlanPublisher.cs
    student/Scm.master.cs
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
  </read_first>
  <action>Per D-01 and D-02, verify and tighten the brownfield route from the published AI activity menu row into the existing student mission page. Keep `Ltype=1` mapped to `~/student/showmission.aspx?lid=` and add or update regression tests that assert the publish core still forces `Ltype=1` and the student menu still routes that type to `showmission.aspx`. Do not add a new AI-specific student route or menu type. If the menu flow needs a focused readability hook for AI activity titles or icons, keep it additive and do not break existing mission entries.</action>
  <acceptance_criteria>
    - `student/Scm.master.cs` contains the route `~/student/showmission.aspx?lid=` for `Ltype=1`
    - `Tests/TeacherRegressionTests/TeacherRegressionTests.cs` contains test names including `ActivityPlanStudentEntry` or `ShowMission`
    - route verification mentions `Ltype=1` and `showmission.aspx`
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter "FullyQualifiedName~ActivityPlan|FullyQualifiedName~ShowMission|FullyQualifiedName~StudentEntry"</automated>
  </verify>
  <done>The published AI activity now has an explicit, regression-pinned student entry route in the existing class menu.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shape the mission page into a readable guided activity experience</name>
  <files>student/showmission.aspx, student/showmission.aspx.cs, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    student/showmission.aspx
    student/showmission.aspx.cs
    App_Code/Common/AIActivityPlanPublishContentBuilder.cs
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
  </read_first>
  <behavior>
    - Test 1: the mission page surfaces learner-facing goal, instructions, or step sections without requiring teacher draft state
    - Test 2: the upload sidebar and existing submission affordances remain present
    - Test 3: missing or non-AI mission content falls back safely instead of crashing the page
  </behavior>
  <action>Per D-03 through D-07, update `student/showmission.aspx` and `student/showmission.aspx.cs` so published AI mission content is presented as a clear learner activity page. Use the smallest brownfield-safe shaping needed to surface the goal, instructions, and ordered steps from `Mission.Mcontent`, while preserving `HiddenMissionRaw`, `Panelworks`, and the existing upload sidebar. Keep the page server-owned and do not fetch teacher drafts, teacher endpoints, or local draft state in the student browser. Add regression tests that lock the presence of the guidance shell and the continued presence of upload affordances on the page.</action>
  <acceptance_criteria>
    - `student/showmission.aspx` contains explicit learner guidance markup or section containers for the published activity content
    - `student/showmission.aspx.cs` still decodes mission content from `Mission.Mcontent` and fails closed when the mission cannot load
    - the page still contains `Panelworks` and existing upload-related controls
    - `Tests/TeacherRegressionTests/TeacherRegressionTests.cs` contains test names including `ShowMission`
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter "FullyQualifiedName~ShowMission|FullyQualifiedName~ActivityPlanStudent"</automated>
  </verify>
  <done>The student page can show a published AI activity as a readable guided mission without losing the legacy upload shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher publish data -> student menu | Previously teacher-owned publish output becomes visible student navigation |
| `student/showmission.aspx.cs` -> student browser | Server-rendered mission content becomes learner guidance on the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I | Student route contract | mitigate | Keep `Ltype=1` on the existing `showmission.aspx` path and pin it with regression tests |
| T-06-02 | T | Mission guidance rendering | mitigate | Render learner guidance from published `Mission.Mcontent` only, never from teacher draft state |
| T-06-03 | D | Mission page fallback | mitigate | Fail closed to existing not-found or raw-content behavior when mission data is absent or malformed |
</threat_model>

<verification>
- Run the targeted `TeacherRegressionTests` filters for the student entry and
  `showmission` source assertions.
- Read `student/Scm.master.cs`, `student/showmission.aspx`, and
  `student/showmission.aspx.cs` to confirm the route and upload shell stayed on
  the existing mission path.
</verification>

<success_criteria>
- A published AI activity is reachable from the existing student class menu.
- The student mission page opens through the existing authenticated route.
- The mission page surfaces guided activity content without breaking the upload
  shell needed for Phase 7.
</success_criteria>

<output>
After completion, create `.planning/phases/06-student-activity-entry-and-guided-experience/06-student-activity-entry-and-guided-experience-01-SUMMARY.md`
</output>
