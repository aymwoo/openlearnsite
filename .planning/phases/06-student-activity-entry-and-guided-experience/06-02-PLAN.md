---
phase: 06-student-activity-entry-and-guided-experience
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - App_Code/Common/AIActivityPlanMissionViewHelper.cs
  - student/showmission.aspx
  - student/showmission.aspx.cs
  - Tests/CommonLogicTests/CommonLogicTests.cs
  - Tests/TeacherRegressionTests/TeacherRegressionTests.cs
autonomous: true
requirements:
  - SAE-02
  - SAE-03
must_haves:
  truths:
    - AI mission guidance is derived from published mission content, not teacher-only draft state.
    - Learner steps render in a deterministic order and fail closed when the AI marker format is absent.
    - Phase 6 ends with both automated source coverage and explicit manual browser validation instructions.
  artifacts:
    - path: App_Code/Common/AIActivityPlanMissionViewHelper.cs
      provides: Mission-content parse or shaping logic for learner guidance.
    - path: student/showmission.aspx
      provides: Final student guidance presentation surface.
    - path: student/showmission.aspx.cs
      provides: Helper integration and safe fallback behavior.
    - path: Tests/CommonLogicTests/CommonLogicTests.cs
      provides: Logic coverage for AI mission guidance parsing or formatting.
  key_links:
    - from: App_Code/Common/AIActivityPlanMissionViewHelper.cs
      to: student/showmission.aspx.cs
      via: helper-based mission shaping.
      pattern: ActivityPlanMissionViewHelper
    - from: student/showmission.aspx.cs
      to: Mission.Mcontent
      via: published mission payload rendering.
      pattern: Mcontent
---

<objective>
Finish the guided learner experience and close the remaining Phase 6 validation
gap with helper-level coverage plus manual UAT instructions.

Purpose: The milestone does not only require that students can open the page.
They must also be able to follow the AI-generated guidance clearly enough to do
the activity without teacher-side context.
Output: helper-backed guidance rendering, focused logic tests, and explicit UAT
coverage for the authenticated student path.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-v1.1-MILESTONE-AUDIT.md
@.planning/phases/06-student-activity-entry-and-guided-experience/06-CONTEXT.md
@.planning/phases/06-student-activity-entry-and-guided-experience/06-RESEARCH.md
@student/showmission.aspx
@student/showmission.aspx.cs
@Tests/CommonLogicTests/CommonLogicTests.cs
@Tests/TeacherRegressionTests/TeacherRegressionTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a helper for AI mission guidance extraction and fallback</name>
  <files>App_Code/Common/AIActivityPlanMissionViewHelper.cs, Tests/CommonLogicTests/CommonLogicTests.cs</files>
  <read_first>
    App_Code/Common/AIActivityPlanPublishContentBuilder.cs
    student/showmission.aspx.cs
    Tests/CommonLogicTests/CommonLogicTests.cs
  </read_first>
  <behavior>
    - Test 1: helper extracts or formats goal and step guidance in stable order
    - Test 2: helper returns a safe fallback when structured AI markers are missing
    - Test 3: helper does not require teacher draft JSON or browser state
  </behavior>
  <action>Per D-04 through D-07, add a small helper such as `AIActivityPlanMissionViewHelper` that works from published `Mission.Mcontent` only. The helper should recognize the Phase 5 AI mission format well enough to surface learner-facing goal, instructions, and ordered steps for the `showmission` page, but it must fail closed when the content does not match the AI activity shape. Add CommonLogic tests that pin the helper behavior and ensure it never depends on teacher-side draft data or browser-owned state.</action>
  <acceptance_criteria>
    - `App_Code/Common/AIActivityPlanMissionViewHelper.cs` exists and references `Mission.Mcontent`-style published content input
    - `Tests/CommonLogicTests/CommonLogicTests.cs` contains test names including `ActivityPlanMissionView` or `ShowMission`
    - helper coverage includes a safe fallback path
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/CommonLogicTests/CommonLogicTests.csproj --filter "FullyQualifiedName~ActivityPlanMissionView|FullyQualifiedName~ShowMission"</automated>
  </verify>
  <done>The student guidance experience has a deterministic server-owned helper instead of ad hoc page-only parsing.</done>
</task>

<task type="auto">
  <name>Task 2: Finish Phase 6 validation with browser-focused UAT support</name>
  <files>student/showmission.aspx, student/showmission.aspx.cs, Tests/TeacherRegressionTests/TeacherRegressionTests.cs</files>
  <read_first>
    student/showmission.aspx
    student/showmission.aspx.cs
    Tests/TeacherRegressionTests/TeacherRegressionTests.cs
    .planning/v1.1-v1.1-MILESTONE-AUDIT.md
  </read_first>
  <action>Integrate the new helper into `student/showmission.aspx.cs`, finalize any remaining learner-facing section markup in `student/showmission.aspx`, and add regression tests that pin the final AI mission guidance shell. Then document explicit manual UAT steps in the phase summary for teacher publish -> student menu entry -> student page open -> readable guidance verification, because the repository has no dedicated student UI regression suite.</action>
  <acceptance_criteria>
    - `student/showmission.aspx.cs` integrates the helper or equivalent server-owned guidance shaping path
    - `student/showmission.aspx` contains the final learner guidance shell and still preserves `Panelworks`
    - `Tests/TeacherRegressionTests/TeacherRegressionTests.cs` contains final source assertions for the guided mission page
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/TeacherRegressionTests/TeacherRegressionTests.csproj --filter "FullyQualifiedName~ShowMission|FullyQualifiedName~ActivityPlanStudent"</automated>
    <manual>Authenticated browser UAT: publish one AI activity as visible, open the course as a student, verify the menu entry appears, open `showmission.aspx`, confirm the goal/instructions/steps are readable, and confirm the upload panel remains available.</manual>
  </verify>
  <done>Phase 6 ends with a guided student experience that is automated where possible and explicitly manually validated where the repo lacks browser regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published mission HTML -> guidance helper | Existing stored content is interpreted into learner guidance |
| server guidance output -> student browser | Learner-facing instructions are presented as actionable classroom steps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | T | Mission guidance parser | mitigate | Parse only the published mission content shape and fail closed when markers are missing |
| T-06-05 | I | Student guidance source | mitigate | Keep guidance server-owned from `Mission.Mcontent`; never read teacher draft JSON in the student flow |
| T-06-06 | R | Phase 6 validation | mitigate | Pair automated helper and source tests with explicit manual browser UAT in the summary artifact |
</threat_model>

<verification>
- Run the targeted `CommonLogicTests` and `TeacherRegressionTests` filters for
  the helper and `showmission` source assertions.
- Manually open one published AI activity as a student to confirm the guidance
  is readable without teacher-side context.
</verification>

<success_criteria>
- Students can view the published goal, instructions, and task steps on the
  activity page.
- Students can follow step-by-step learner guidance from published mission
  content alone.
- Phase 6 has both automated coverage and explicit manual validation steps.
</success_criteria>

<output>
After completion, create `.planning/phases/06-student-activity-entry-and-guided-experience/06-student-activity-entry-and-guided-experience-02-SUMMARY.md`
</output>
