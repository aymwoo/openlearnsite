{
  "name": "learnsite-wz-tests",
  "version": "1.0.0",
  "private": true,
  "description": "LearnSite 前端测试套件 (Playwright E2E + Vitest 单元测试)",
  "scripts": {
    "test:e2e": "npx playwright test",
    "test:unit": "npx vitest run",
    "test:all": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@vitest/coverage-v8": "^3.0.0",
    "jsdom": "^25.0.0",
    "vitest": "^3.2.4"
  }
}
